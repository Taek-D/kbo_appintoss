# 커밋 → 푸시 → PR 생성

변경사항을 커밋하고 PR을 생성합니다.

## 실행 순서

1. `git status`로 변경사항 확인
2. `pnpm typecheck` 타입 검사
3. `pnpm lint` 린트 검사
4. `pnpm test --run` 테스트 실행
5. 모두 통과하면 커밋 메시지 작성 (한국어, 동사형)
6. 푸시 후 PR 생성

## 커밋 메시지 규칙
- 한국어 사용
- 동사형 시작 (추가:, 수정:, 개선:, 제거:)
- 예: "추가: 크롤러 모듈 TDD 테스트"

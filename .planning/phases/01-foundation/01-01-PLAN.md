---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.mts
  - vitest.setup.ts
  - src/types/user.ts
  - src/types/game.ts
  - src/types/toss.ts
  - src/lib/supabase/client.ts
  - src/lib/supabase/server.ts
  - src/lib/logger.ts
  - supabase/migrations/20260404000000_init_schema.sql
  - .env.local
  - .env.example
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "Supabase에 users, games, push_logs 테이블 DDL이 마이그레이션 파일로 존재한다"
    - "프로젝트가 pnpm dev로 정상 기동되고 pnpm test --run이 통과한다"
    - "공유 타입(User, Game, TeamCode, Toss 관련)이 types/ 디렉토리에 정의되어 있다"
    - "토스 Push 메시지 템플릿 검수가 신청되었다"
  artifacts:
    - path: "supabase/migrations/20260404000000_init_schema.sql"
      provides: "users, games, push_logs 테이블 DDL + 인덱스 + updated_at 트리거"
      contains: "create table public.users"
    - path: "src/types/user.ts"
      provides: "TeamCode 리터럴 유니온, KBO_TEAMS 상수, User 타입"
      exports: ["TeamCode", "KBO_TEAMS", "User"]
    - path: "src/types/game.ts"
      provides: "GameStatus, Game 타입"
      exports: ["GameStatus", "Game"]
    - path: "src/types/toss.ts"
      provides: "TossAuthResponse, TossUserInfo 타입"
      exports: ["TossAuthResponse", "TossUserInfo"]
    - path: "vitest.config.mts"
      provides: "Vitest 테스트 설정"
    - path: "src/lib/supabase/server.ts"
      provides: "서버용 Supabase 클라이언트"
      exports: ["createServerSupabaseClient"]
    - path: "src/lib/supabase/client.ts"
      provides: "브라우저용 Supabase 클라이언트"
      exports: ["createBrowserSupabaseClient"]
  key_links:
    - from: "src/lib/supabase/server.ts"
      to: "@supabase/ssr"
      via: "createServerClient import"
      pattern: "import.*createServerClient.*@supabase/ssr"
    - from: "src/types/user.ts"
      to: "supabase/migrations/20260404000000_init_schema.sql"
      via: "TeamCode 리터럴이 DB team_code 컬럼과 일치"

user_setup:
  - service: supabase
    why: "DB 호스팅 및 PostgreSQL"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Settings -> API -> anon public key"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Settings -> API -> service_role key"
  - service: toss-apps-in-toss
    why: "토스 미니앱 등록 및 mTLS 인증서"
    env_vars:
      - name: TOSS_MTLS_CERT
        source: "앱인토스 콘솔 -> 인증서 관리 -> 인증서 다운로드 (base64 인코딩)"
      - name: TOSS_MTLS_KEY
        source: "앱인토스 콘솔 -> 인증서 관리 -> 개인키 다운로드 (base64 인코딩)"
---

<objective>
Next.js 프로젝트 스캐폴딩, Supabase DB 스키마 마이그레이션, 공유 타입 정의, 테스트 인프라 구축, 토스 Push 템플릿 검수 신청

Purpose: Phase 1의 모든 후속 플랜(Auth 모듈, UI)이 의존하는 프로젝트 기반과 데이터 계약을 확립한다. 템플릿 검수는 개발과 병렬로 진행해야 KBO 시즌 타이밍을 놓치지 않는다.
Output: 실행 가능한 Next.js 프로젝트 + DB 마이그레이션 파일 + 공유 타입 + Vitest 설정 + 템플릿 검수 신청 완료
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Next.js 프로젝트 스캐폴딩 + Supabase/Vitest 설정 + DB 마이그레이션 + 공유 타입 정의</name>
  <files>
    package.json, tsconfig.json, tailwind.config.ts,
    vitest.config.mts, vitest.setup.ts,
    src/types/user.ts, src/types/game.ts, src/types/toss.ts,
    src/lib/supabase/client.ts, src/lib/supabase/server.ts, src/lib/logger.ts,
    supabase/migrations/20260404000000_init_schema.sql,
    .env.local, .env.example,
    src/app/layout.tsx, src/app/page.tsx
  </files>
  <action>
    1. Next.js 프로젝트 생성 (이미 존재하지 않으면):
       `npx create-next-app@latest . --typescript --tailwind --app --src-dir --use-pnpm`
       이미 존재하면 스킵.

    2. 의존성 설치:
       ```
       pnpm add @supabase/supabase-js @supabase/ssr zod pino
       pnpm add -D vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/user-event msw vite-tsconfig-paths
       ```

    3. Vitest 설정 생성:
       - `vitest.config.mts`: tsconfigPaths + react 플러그인, jsdom 환경, globals: true, setupFiles: ['./vitest.setup.ts']
       - `vitest.setup.ts`: `import '@testing-library/jest-dom/vitest'`
       - package.json에 `"test": "vitest"` 스크립트 확인 (없으면 추가)

    4. 공유 타입 정의 (CLAUDE.md: type 선호, enum 절대 금지):

       `src/types/user.ts`:
       - `TeamCode` = 문자열 리터럴 유니온 ('HH'|'OB'|'LG'|'KT'|'SS'|'NC'|'SK'|'LT'|'WO'|'KI')
       - `KBO_TEAMS: Array<{ code: TeamCode; name: string; logo: string }>` 상수 (logo는 `/teams/{code}.png` 경로)
       - `User` 타입 = { id: string; toss_user_key: string; team_code: TeamCode | null; subscribed: boolean; created_at: string; updated_at: string }

       `src/types/game.ts`:
       - `GameStatus` = 'scheduled' | 'playing' | 'finished' | 'cancelled'
       - `Game` 타입 = DB games 테이블 구조 매핑

       `src/types/toss.ts`:
       - `TossReferrer` = 'sandbox' | 'DEFAULT'
       - `TossAuthResponse` = { accessToken, refreshToken, tokenType, expiresIn }
       - `TossUserInfo` = { userKey: string }

    5. Supabase 클라이언트 설정:
       - `src/lib/supabase/server.ts`: @supabase/ssr의 `createServerClient` 사용. cookies() 연동. RESEARCH.md 코드 예시 참고.
       - `src/lib/supabase/client.ts`: @supabase/ssr의 `createBrowserClient` 사용. NEXT_PUBLIC_ 환경변수 참조.

    6. 로거 설정:
       - `src/lib/logger.ts`: pino 기반 로거. 개발 환경에서는 pino-pretty. console.log 대체.

    7. DB 마이그레이션 SQL 작성:
       - `supabase/migrations/20260404000000_init_schema.sql`: RESEARCH.md Pattern 2의 SQL 그대로 사용.
       - users 테이블 (id UUID PK, toss_user_key TEXT UNIQUE, team_code TEXT, subscribed BOOLEAN default false, created_at, updated_at)
       - games 테이블 (id UUID PK, game_date DATE, home_team TEXT, away_team TEXT, status TEXT default 'scheduled', home_score INT, away_score INT, inning_data JSONB, started_at, finished_at, is_notified BOOLEAN default false, created_at, updated_at)
       - push_logs 테이블 (id BIGINT identity PK, user_id UUID FK, game_id UUID FK, status TEXT, error_message TEXT, sent_at)
       - 인덱스: idx_games_date_status, idx_users_toss_user_key, idx_push_logs_game_id
       - updated_at 자동 갱신 트리거 (users, games)

    8. 환경변수:
       - `.env.example`: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, TOSS_MTLS_CERT, TOSS_MTLS_KEY 템플릿
       - `.env.local`: .env.example 복사 (실제 값은 비워둠)
       - `.gitignore`에 `.env.local` 포함 확인

    주의사항:
    - @supabase/auth-helpers-nextjs 사용 금지 (deprecated). 반드시 @supabase/ssr 사용.
    - enum 절대 금지. 문자열 리터럴 유니온만 사용.
    - console.log 금지. pino 로거 사용.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test --run 2>&1 | head -20</automated>
  </verify>
  <done>
    - pnpm dev로 Next.js 개발 서버 정상 기동
    - pnpm typecheck 에러 없음
    - pnpm test --run 실행 가능 (테스트 0개 또는 setup 테스트 통과)
    - supabase/migrations/ 에 DDL 파일 존재
    - src/types/ 에 user.ts, game.ts, toss.ts 존재하고 export 확인
    - src/lib/supabase/ 에 server.ts, client.ts 존재
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: 토스 Push 메시지 템플릿 검수 신청</name>
  <files>N/A</files>
  <action>
    유저가 앱인토스 콘솔(https://developers-apps-in-toss.toss.im)에서 직접 수행:
    1. 앱 등록 (아직 안 했다면)
    2. Push 메시지 템플릿 등록:
       - 제목: "[팀명] 경기가 끝났습니다"
       - 내용: "[홈팀] [홈스코어] : [어웨이스코어] [어웨이팀] 결과를 확인해보세요"
    3. 검수 신청 완료

    기능성 메시지 원칙 준수:
    - 순수 정보 전달만 포함
    - 특수문자/이모지 최소화
    - 행동 유도 문구 금지 ("지금 바로!", "놓치지 마세요!" 등)
  </action>
  <verify>앱인토스 콘솔에서 검수 신청 상태 확인</verify>
  <done>토스 Push 메시지 템플릿 검수 신청 완료 (검수 통과는 비동기 -- 개발과 병렬 진행)</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` 에러 없음
- `pnpm test --run` 실행 가능
- `supabase/migrations/20260404000000_init_schema.sql` 파일에 3개 테이블 DDL 포함
- `src/types/` 디렉토리에 user.ts, game.ts, toss.ts 존재
- `.env.example`에 필요한 환경변수 키 모두 포함
</verification>

<success_criteria>
- Next.js 프로젝트가 pnpm dev로 정상 기동됨
- Vitest가 pnpm test --run으로 실행 가능
- DB 마이그레이션 SQL이 유효한 PostgreSQL DDL로 작성됨
- 공유 타입이 후속 플랜에서 import 가능한 상태
- 토스 Push 메시지 템플릿 검수 신청 완료
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>

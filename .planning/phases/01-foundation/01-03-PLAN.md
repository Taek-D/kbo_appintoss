---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/(main)/page.tsx
  - src/app/(main)/team-select/page.tsx
  - src/app/(main)/layout.tsx
  - src/components/TeamGrid.tsx
  - src/components/ConfirmModal.tsx
  - src/components/SubscriptionControl.tsx
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - SUB-01
  - SUB-02

must_haves:
  truths:
    - "유저가 로그인 화면에서 토스 로그인 버튼을 눌러 서비스에 진입할 수 있다 (AUTH-01)"
    - "유저가 5x2 그리드에서 KBO 구단을 선택하고 확인하면 응원팀이 저장된다 (AUTH-02)"
    - "재진입 시 메인 화면에 이전 응원팀이 표시된다 (AUTH-03)"
    - "메인 화면에서 응원팀을 탭하면 변경/해제 옵션이 나타난다 (AUTH-04, SUB-01)"
    - "구독 해제 시 확인 모달이 표시되고 확인하면 해제된다 (SUB-01)"
    - "응원팀에 오늘 경기가 없으면 '오늘 경기 없음' 안내가 표시된다 (SUB-02)"
  artifacts:
    - path: "src/app/(auth)/login/page.tsx"
      provides: "토스 로그인 화면 — 로그인 버튼 + 한 줄 설명"
      min_lines: 20
    - path: "src/app/(main)/team-select/page.tsx"
      provides: "응원팀 선택 화면 — 5x2 그리드 + 확인 단계"
      min_lines: 40
    - path: "src/app/(main)/page.tsx"
      provides: "메인 화면 — 응원팀 표시 + 오늘 경기 정보 or '경기 없음'"
      min_lines: 40
    - path: "src/components/TeamGrid.tsx"
      provides: "10개 구단 5x2 그리드 컴포넌트"
      exports: ["TeamGrid"]
    - path: "src/components/ConfirmModal.tsx"
      provides: "확인 모달 — 응원팀 확인, 구독 해제 확인에 재사용"
      exports: ["ConfirmModal"]
    - path: "src/components/SubscriptionControl.tsx"
      provides: "구독 관리 — 현재 응원팀 표시 + 변경/해제 옵션"
      exports: ["SubscriptionControl"]
  key_links:
    - from: "src/app/(auth)/login/page.tsx"
      to: "/api/auth/login"
      via: "appLogin() → fetch POST"
      pattern: "fetch.*api/auth/login"
    - from: "src/app/(main)/team-select/page.tsx"
      to: "/api/subscription"
      via: "fetch PUT with team_code"
      pattern: "fetch.*api/subscription"
    - from: "src/components/SubscriptionControl.tsx"
      to: "/api/subscription"
      via: "fetch DELETE for unsubscribe"
      pattern: "fetch.*api/subscription.*DELETE"
    - from: "src/app/(main)/page.tsx"
      to: "/api/auth/me"
      via: "fetch GET for current user"
      pattern: "fetch.*api/auth/me"
---

<objective>
온보딩 UI (로그인 + 응원팀 선택) + 메인 화면 + 구독 관리 UX 구현

Purpose: 유저가 토스 로그인으로 진입하여 응원팀을 선택하고, 메인 화면에서 응원팀 정보를 확인하고, 변경/해제할 수 있는 전체 프론트엔드 플로우를 완성한다.
Output: 로그인 → 팀 선택 → 메인 화면 → 변경/해제의 전체 유저 플로우가 동작하는 UI
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 타입 -->
From src/types/user.ts:
```typescript
export type TeamCode = 'HH' | 'OB' | 'LG' | 'KT' | 'SS' | 'NC' | 'SK' | 'LT' | 'WO' | 'KI'
export const KBO_TEAMS: Array<{ code: TeamCode; name: string; logo: string }>
export type User = {
  id: string; toss_user_key: string; team_code: TeamCode | null;
  subscribed: boolean; created_at: string; updated_at: string;
}
```

<!-- Plan 02에서 생성된 API Routes -->
API Contracts:
- POST /api/auth/login { authorizationCode, referrer } → 200 { success, user } + session cookie
- GET /api/auth/me → 200 { user } | 401
- PUT /api/subscription { team_code } → 200 { success, user }
- DELETE /api/subscription → 200 { success }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 로그인 화면 + 응원팀 선택 화면 + 공유 컴포넌트</name>
  <files>
    src/app/(auth)/login/page.tsx,
    src/app/(main)/team-select/page.tsx,
    src/components/TeamGrid.tsx,
    src/components/ConfirmModal.tsx
  </files>
  <action>
    1. `src/components/ConfirmModal.tsx` — 재사용 가능한 확인 모달:
       - Props: { isOpen, title, message, confirmText, cancelText, onConfirm, onCancel }
       - 토스 스타일: 화이트 배경, 둥근 모서리(rounded-2xl), 하단 버튼 2개
       - 백드롭 클릭으로 닫기
       - 응원팀 확인("OO팀을 응원할까요?")과 구독 해제("알림을 그만 받을까요?")에 재사용

    2. `src/components/TeamGrid.tsx` — 10개 구단 그리드:
       - Props: { onSelect: (code: TeamCode) => void, selectedCode?: TeamCode }
       - KBO_TEAMS 상수 import하여 5x2 그리드 렌더링 (grid-cols-5 grid-rows-2)
       - 각 카드: 팀 로고 이미지(public/teams/{code}.png) + 팀명
       - 선택된 팀 하이라이트(border 색상 변경)
       - Tailwind: gap-3, 각 카드 rounded-xl, shadow-sm, hover 효과
       - 팀 로고 이미지가 없으면 팀 코드 텍스트로 대체 (Image onError fallback)

    3. `src/app/(auth)/login/page.tsx` — 로그인 화면:
       - 사용자 결정: "토스 로그인 버튼 + 간단한 한 줄 설명만 (비주얼/일러스트 없음)"
       - 레이아웃: 화면 중앙 정렬, 서비스명 "KBO 야구 알리미", 한 줄 설명 "응원팀 경기 결과를 바로 알려드려요"
       - 토스 로그인 버튼: "토스로 시작하기" 텍스트, 토스 블루(#0064FF) 배경
       - 클릭 시:
         a. `@apps-in-toss/web-framework`의 `appLogin()` 호출
         b. 반환된 { authorizationCode, referrer }를 POST /api/auth/login으로 전달
         c. 성공 시: user.team_code 있으면 '/' (메인), 없으면 '/team-select' 로 router.push
         d. 실패 시: 에러 토스트 표시 (alert 대신 간단한 인라인 에러 메시지)
       - 개발 환경 분기: `process.env.NODE_ENV === 'development'`면 appLogin() 대신 mock authCode로 로그인 (토스 WebView 외부에서 테스트 불가하므로)
       - 토스 스타일: 화이트 배경, 깨끗한 여백

    4. `src/app/(main)/team-select/page.tsx` — 응원팀 선택 화면:
       - 상단 "응원팀을 선택해주세요" 타이틀
       - TeamGrid 컴포넌트 사용
       - 팀 탭 시 ConfirmModal 열기: "OO팀을 응원할까요?" + 확인/취소 버튼
       - 확인 클릭 시: PUT /api/subscription { team_code } 호출 → 성공 시 '/' 로 이동
       - 이미 응원팀이 있는 경우(변경 모드): 현재 팀 하이라이트 표시 (selectedCode prop)
       - 로딩 상태: 확인 버튼 disable + "저장 중..." 텍스트

    토스 디자인 스타일 공통:
    - 배경 white (#FFFFFF)
    - 텍스트 gray-900 (#111111)
    - 서브텍스트 gray-500 (#6B7280)
    - 카드/모달 rounded-2xl
    - 버튼 rounded-xl, py-3
    - 간격 넉넉하게 (p-5, gap-4)
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>
    - 로그인 화면에 토스 로그인 버튼이 렌더링됨
    - 팀 선택 화면에 10개 구단이 5x2 그리드로 표시됨
    - 팀 탭 시 확인 모달이 열리고 확인하면 API 호출 후 메인으로 이동
    - 개발 환경에서 mock 로그인 가능
    - pnpm typecheck 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: 메인 화면 + 구독 관리 + "오늘 경기 없음" 안내</name>
  <files>
    src/app/(main)/page.tsx,
    src/app/(main)/layout.tsx,
    src/components/SubscriptionControl.tsx
  </files>
  <action>
    1. `src/components/SubscriptionControl.tsx` — 구독 관리 컴포넌트:
       - Props: { user: User, onTeamChange: () => void, onUnsubscribe: () => void }
       - 현재 응원팀 표시: 팀 로고 + 팀명 + "알림 받는 중" 뱃지
       - 탭하면 바텀시트 또는 드롭다운: "응원팀 변경", "알림 끄기" 옵션
       - "알림 끄기" 클릭 시 ConfirmModal: "알림을 그만 받을까요?" + 확인/취소
       - 확인 시 DELETE /api/subscription 호출 → subscribed=false 반영
       - "응원팀 변경" 클릭 시 /team-select로 이동 (현재 팀 코드를 query param으로 전달)

    2. `src/app/(main)/layout.tsx` — 메인 레이아웃:
       - 인증 가드: GET /api/auth/me 호출 → 401이면 /login 으로 리다이렉트
       - team_code가 null이면 /team-select로 리다이렉트
       - 정상이면 children 렌더링

    3. `src/app/(main)/page.tsx` — 메인 화면:
       - GET /api/auth/me로 현재 유저 정보 로드
       - 상단: SubscriptionControl 컴포넌트 (현재 응원팀 + 변경/해제)
       - 하단: 오늘 경기 정보 영역
         - Phase 2에서 games API가 구현되기 전이므로, 현재는 임시 UI:
           "오늘 경기 없음" 안내 화면을 기본으로 표시 (SUB-02)
         - "오늘 경기 없음" 디자인: 아이콘(야구공 또는 캘린더) + "오늘은 [팀명] 경기가 없어요" 텍스트
         - Phase 2에서 games 데이터 연동 시 이 영역을 교체할 예정 (TODO 주석으로 명시)
       - 로딩 상태: 스켈레톤 UI (rounded-2xl placeholder)
       - 에러 상태: "정보를 불러올 수 없습니다. 다시 시도해주세요." + 재시도 버튼
       - 토스 스타일: 화이트 배경, 카드 형태로 정보 표시

    주의사항:
    - appLogin()은 토스 WebView에서만 동작. 개발 환경 분기 필수.
    - localStorage 사용 금지 (iOS ITP 이슈). 세션은 httpOnly cookie로 관리 (Plan 02에서 구현 완료).
    - 이미지 없는 팀 로고는 graceful fallback 처리.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - 메인 화면에 현재 응원팀이 표시됨
    - SubscriptionControl에서 변경/해제 옵션 접근 가능
    - 구독 해제 시 확인 모달 표시 후 API 호출
    - "오늘 경기 없음" 안내 화면 표시됨
    - pnpm build 성공 (SSR 에러 없음)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 전체 온보딩 플로우 시각 검증</name>
  <files>N/A</files>
  <action>
    유저가 전체 온보딩 플로우를 시각적으로 검증한다:
    1. pnpm dev로 개발 서버 시작
    2. http://localhost:3000/login 접속하여 로그인 화면 확인
    3. mock 로그인 후 팀 선택 → 메인 화면 → 변경/해제 전체 플로우 테스트
  </action>
  <verify>
    유저가 다음을 확인:
    - 로그인 화면: 토스 로그인 버튼 + 한 줄 설명만 (비주얼 없음), 토스 스타일
    - 팀 선택 화면: 10개 구단 5x2 그리드, 팀 탭 시 확인 모달
    - 메인 화면: 응원팀 표시, "오늘 경기 없음" 안내
    - 구독 관리: 응원팀 변경/해제 옵션, 해제 시 확인 모달
  </verify>
  <done>"approved" 입력 시 완료. 수정 사항이 있으면 피드백 반영 후 재검증.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` — 타입 에러 없음
- `pnpm build` — 빌드 성공 (SSR 에러 없음)
- 전체 온보딩 플로우가 개발 서버에서 동작
</verification>

<success_criteria>
- 로그인 → 팀 선택 → 메인 화면의 전체 플로우가 동작
- 응원팀 변경/구독 해제 UX가 사용자 결정대로 구현됨
- "오늘 경기 없음" 안내 화면이 표시됨
- 토스 디자인 스타일 (화이트톤, 둥근 모서리) 적용
- pnpm build 성공
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>

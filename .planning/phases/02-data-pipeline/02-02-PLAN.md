---
phase: 02-data-pipeline
plan: 02
type: tdd
wave: 2
depends_on: [02-01]
files_modified:
  - src/app/api/cron/poll/route.ts
  - src/app/api/cron/poll/__tests__/route.test.ts
  - scripts/setup-qstash.ts
  - .env.example
autonomous: true
requirements: [DATA-01, DATA-02, DATA-04]
user_setup:
  - service: upstash-qstash
    why: "QStash Cron으로 3분 간격 폴링 트리거 (D-07, D-08)"
    env_vars:
      - name: QSTASH_TOKEN
        source: "Upstash Console -> QStash -> Settings -> Token"
      - name: QSTASH_CURRENT_SIGNING_KEY
        source: "Upstash Console -> QStash -> Settings -> Signing Keys -> Current"
      - name: QSTASH_NEXT_SIGNING_KEY
        source: "Upstash Console -> QStash -> Settings -> Signing Keys -> Next"
    dashboard_config:
      - task: "Upstash 계정 생성 및 QStash 활성화"
        location: "https://console.upstash.com -> QStash"

must_haves:
  truths:
    - "QStash가 POST /api/cron/poll을 호출하면 kbo-game 크롤링 + DB 상태 전이 감지가 실행된다"
    - "유효하지 않은 QStash 서명으로 호출하면 401이 반환된다"
    - "경기 시간대(14~22시 KST) 외에 호출하면 크롤링 없이 200이 반환된다 (per D-09)"
    - "크롤링 실패 시 500이 아닌 200을 반환하여 QStash 자동 재시도를 방지한다 (per D-01)"
    - "setup-qstash.ts 스크립트로 3분 간격 Cron Schedule을 등록할 수 있다"
  artifacts:
    - path: "src/app/api/cron/poll/route.ts"
      provides: "QStash 폴링 엔드포인트 — 서명 검증 + KST 시간 체크 + crawler 호출"
      exports: ["POST"]
    - path: "src/app/api/cron/poll/__tests__/route.test.ts"
      provides: "폴링 엔드포인트 테스트"
    - path: "scripts/setup-qstash.ts"
      provides: "QStash Cron Schedule 등록 스크립트"
    - path: ".env.example"
      provides: "QStash 환경변수 템플릿 추가"
  key_links:
    - from: "src/app/api/cron/poll/route.ts"
      to: "src/backend/modules/crawler/index.ts"
      via: "import { fetchTodayGames, syncGames }"
      pattern: "import.*fetchTodayGames.*syncGames"
    - from: "src/app/api/cron/poll/route.ts"
      to: "@upstash/qstash"
      via: "Receiver 서명 검증"
      pattern: "new Receiver"
    - from: "src/app/api/cron/poll/route.ts"
      to: "src/lib/logger.ts"
      via: "에러/결과 로깅"
      pattern: "logger\\.(error|info|warn)"
    - from: "scripts/setup-qstash.ts"
      to: "@upstash/qstash"
      via: "Client.schedules.create()"
      pattern: "schedules\\.create"
---

<objective>
QStash 폴링 API Route를 TDD로 구현하여 3분마다 CrawlerService + GameRepository를 호출하고, QStash Cron Schedule 등록 스크립트를 작성한다.

Purpose: Plan 01에서 구현한 데이터 레이어(crawler 모듈)를 실제로 3분 간격으로 트리거하는 엔드포인트. QStash 서명 검증으로 보안을 확보하고, KST 경기 시간대 체크로 불필요한 크롤링을 방지한다.
Output: 폴링 API Route + 테스트 + QStash 설정 스크립트 + 환경변수 문서화
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-pipeline/02-CONTEXT.md
@.planning/phases/02-data-pipeline/02-RESEARCH.md
@.planning/phases/02-data-pipeline/02-01-SUMMARY.md

@src/backend/modules/crawler/index.ts
@src/backend/modules/crawler/crawler-service.ts
@src/backend/modules/crawler/game-repository.ts
@src/types/crawler.ts
@src/types/game.ts
@src/lib/logger.ts
@src/lib/supabase/server.ts
@src/app/api/auth/login/route.ts
@.env.example

<interfaces>
<!-- Plan 01에서 생성된 crawler 모듈 public API -->

From src/backend/modules/crawler/index.ts:
```typescript
export { fetchTodayGames } from './crawler-service'
export { syncGames } from './game-repository'
export { mapKboStatusToDb } from './game-state-mapper'
```

From src/types/crawler.ts:
```typescript
export type CrawlerResult =
  | { success: true; games: CrawlerGame[] }
  | { success: false; error: Error }

export type StateTransition = {
  gameId: string
  kboGameId: string
  fromStatus: GameStatus
  toStatus: GameStatus
  game: CrawlerGame
}
```

From src/lib/logger.ts:
```typescript
export const logger: pino.Logger
```

@upstash/qstash Receiver API:
```typescript
import { Receiver } from '@upstash/qstash'

const receiver = new Receiver({
  currentSigningKey: process.env.QSTASH_CURRENT_SIGNING_KEY!,
  nextSigningKey: process.env.QSTASH_NEXT_SIGNING_KEY!,
})

// 서명 검증
await receiver.verify({
  body: string,
  signature: string,  // req.headers.get('Upstash-Signature')
})
```

@upstash/qstash Client API:
```typescript
import { Client } from '@upstash/qstash'

const client = new Client({ token: process.env.QSTASH_TOKEN! })
await client.schedules.create({
  destination: 'https://your-app.vercel.app/api/cron/poll',
  cron: '*/3 5-13 * * *',  // UTC 05~13시 = KST 14~22시
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QStash 폴링 API Route TDD</name>
  <files>
    src/app/api/cron/poll/route.ts,
    src/app/api/cron/poll/__tests__/route.test.ts
  </files>
  <read_first>
    src/backend/modules/crawler/index.ts,
    src/backend/modules/crawler/crawler-service.ts,
    src/backend/modules/crawler/game-repository.ts,
    src/types/crawler.ts,
    src/lib/logger.ts,
    src/app/api/auth/login/route.ts,
    .env.example
  </read_first>
  <behavior>
    Poll Route (route.test.ts):
    - Test 1: 유효한 QStash 서명 + 경기 시간대 내(14~22 KST) + 크롤링 성공 + 상태 전이 있음 -> 200 OK + syncGames 호출됨
    - Test 2: Upstash-Signature 헤더가 없으면 401 Unauthorized
    - Test 3: QStash 서명 검증 실패(Receiver.verify throws) -> 401 Invalid signature
    - Test 4: 경기 시간대 밖(KST 13시) -> 200 + fetchTodayGames 호출되지 않음 (per D-09)
    - Test 5: 크롤링 실패(CrawlerResult.success === false) -> 200 반환 + logger.error 호출 (per D-01, QStash 재시도 방지)
    - Test 6: 크롤링 성공 + 경기 없음(빈 배열) -> 200 + syncGames([]) 호출
    - Test 7: 크롤링 성공 + 상태 전이 없음 -> 200 + logger.info에 transitionCount: 0
  </behavior>
  <action>
    **1단계: RED — route.test.ts 작성**

    모킹 전략:
    - `vi.mock('@upstash/qstash')` -> Receiver 클래스 모킹. `verify` 메서드를 mockResolvedValue(undefined)로 기본 설정
    - `vi.mock('@/backend/modules/crawler')` -> fetchTodayGames, syncGames 모킹
    - `vi.mock('@/lib/logger')` -> logger.error, logger.info, logger.warn 모킹

    KST 시간 제어: `vi.useFakeTimers()` + `vi.setSystemTime(new Date('2026-04-05T07:00:00Z'))` (UTC 07시 = KST 16시, 경기 시간대)

    Request 생성 헬퍼:
    ```typescript
    function createMockRequest(options?: { signature?: string; body?: string }): Request {
      const headers = new Headers()
      if (options?.signature) {
        headers.set('Upstash-Signature', options.signature)
      }
      return new Request('https://app.vercel.app/api/cron/poll', {
        method: 'POST',
        headers,
        body: options?.body ?? '',
      })
    }
    ```

    Test 1~7 (behavior 참조) 작성.
    `pnpm test --run src/app/api/cron/poll` -> FAIL 확인.
    커밋: `test(02-02): 추가: QStash 폴링 API Route 실패 테스트`

    **2단계: GREEN — route.ts 구현**

    ```typescript
    import { Receiver } from '@upstash/qstash'
    import { fetchTodayGames, syncGames } from '@/backend/modules/crawler'
    import { logger } from '@/lib/logger'

    const receiver = new Receiver({
      currentSigningKey: process.env.QSTASH_CURRENT_SIGNING_KEY!,
      nextSigningKey: process.env.QSTASH_NEXT_SIGNING_KEY!,
    })

    export async function POST(req: Request): Promise<Response> {
      // 1. QStash 서명 검증 (D-07)
      const signature = req.headers.get('Upstash-Signature')
      if (!signature) {
        return new Response('Unauthorized', { status: 401 })
      }

      const body = await req.text()
      try {
        await receiver.verify({ body, signature })
      } catch {
        return new Response('Invalid signature', { status: 401 })
      }

      // 2. 경기 시간대 체크 (D-09: 14~22시 KST)
      const now = new Date()
      const kstHour = new Date(
        now.toLocaleString('en-US', { timeZone: 'Asia/Seoul' })
      ).getHours()

      if (kstHour < 14 || kstHour >= 22) {
        logger.info({ kstHour }, 'Outside game hours, skipping poll')
        return new Response('Outside game hours', { status: 200 })
      }

      // 3. 크롤링 (DATA-01)
      const result = await fetchTodayGames()

      if (!result.success) {
        // D-01: 실패 시 다음 폴링에서 재시도. 200 반환하여 QStash 자동 재시도 방지
        // D-02: logger로 에러 기록
        logger.error({ error: result.error.message }, 'Crawling failed, will retry next poll')
        return new Response('Crawl failed', { status: 200 })
      }

      // 4. DB 동기화 + 상태 전이 감지 (DATA-02)
      const transitions = await syncGames(result.games)

      // Phase 3 Handoff Contract:
      // transitions(StateTransition[])는 Phase 3에서 Push 알림 트리거로 사용된다.
      // Phase 3 구현 시 이 지점에서 transitions를 순회하며:
      //   - toStatus === 'playing'  -> 경기 시작 알림 (is_notified_start 체크)
      //   - toStatus === 'finished' -> 경기 종료 알림 (is_notified_finish 체크)
      //   - toStatus === 'cancelled' -> 경기 취소 알림 (is_notified_cancel 체크)
      // 각 전이에 대해 해당 팀 구독자를 조회하고 Push 발송 후 is_notified_* 플래그를 업데이트한다.
      // transitions의 gameId(DB uuid)로 games 테이블에서 is_notified_* 상태를 조회/갱신한다.
      logger.info(
        {
          gamesCount: result.games.length,
          transitionCount: transitions.length,
          transitions: transitions.map(t => ({
            from: t.fromStatus,
            to: t.toStatus,
            teams: `${t.game.homeTeam} vs ${t.game.awayTeam}`,
          })),
        },
        'Poll complete'
      )

      return new Response('OK', { status: 200 })
    }
    ```

    `pnpm test --run src/app/api/cron/poll` -> 전부 PASS.
    커밋: `feat(02-02): 구현: QStash 폴링 API Route + 서명 검증 + KST 시간 체크`
  </action>
  <verify>
    <automated>pnpm test --run src/app/api/cron/poll</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/cron/poll/route.ts가 존재하고 `export async function POST`를 포함한다
    - src/app/api/cron/poll/route.ts가 `new Receiver`를 포함한다 (@upstash/qstash 서명 검증)
    - src/app/api/cron/poll/route.ts가 `fetchTodayGames`와 `syncGames`를 import한다
    - src/app/api/cron/poll/route.ts가 `kstHour < 14 || kstHour >= 22` 조건을 포함한다 (per D-09)
    - src/app/api/cron/poll/route.ts가 크롤링 실패 시 `status: 200`을 반환한다 (QStash 재시도 방지)
    - src/app/api/cron/poll/route.ts에 Phase 3 Handoff Contract 주석이 포함된다
    - src/app/api/cron/poll/__tests__/route.test.ts가 최소 6개 테스트를 포함한다
    - `pnpm test --run src/app/api/cron/poll` 종료 코드 0
  </acceptance_criteria>
  <done>
    QStash 폴링 API Route가 서명 검증, KST 시간대 체크, 크롤링, DB 동기화, 상태 전이 감지를 순차적으로 수행하고, 크롤링 실패 시 200을 반환하여 QStash 자동 재시도를 방지하며, Phase 3 Handoff Contract가 문서화되어 있고, 모든 테스트가 통과한다.
  </done>
</task>

<task type="auto">
  <name>Task 2: QStash 설정 스크립트 + 환경변수 문서화 + 통합 검증</name>
  <files>
    scripts/setup-qstash.ts,
    .env.example
  </files>
  <read_first>
    .env.example,
    package.json,
    src/app/api/cron/poll/route.ts
  </read_first>
  <action>
    **1단계: QStash Cron Schedule 등록 스크립트 (scripts/setup-qstash.ts)**

    1회 실행 스크립트 — 배포 후 QStash Dashboard에서 실행하거나 `npx tsx scripts/setup-qstash.ts`로 실행:

    ```typescript
    /**
     * QStash Cron Schedule 등록 스크립트
     * 배포 후 1회 실행: npx tsx scripts/setup-qstash.ts
     *
     * 필요 환경변수:
     *   QSTASH_TOKEN - Upstash Console -> QStash -> Settings -> Token
     *   APP_URL - Vercel 배포 URL (예: https://kbo-game.vercel.app)
     */
    import { Client } from '@upstash/qstash'

    const QSTASH_TOKEN = process.env.QSTASH_TOKEN
    const APP_URL = process.env.APP_URL

    if (!QSTASH_TOKEN) {
      console.error('QSTASH_TOKEN 환경변수가 필요합니다')
      process.exit(1)
    }

    if (!APP_URL) {
      console.error('APP_URL 환경변수가 필요합니다 (예: https://kbo-game.vercel.app)')
      process.exit(1)
    }

    const client = new Client({ token: QSTASH_TOKEN })

    async function main() {
      const schedule = await client.schedules.create({
        destination: `${APP_URL}/api/cron/poll`,
        cron: '*/3 5-13 * * *',  // UTC 05~13시 = KST 14~22시 (per D-09)
      })

      console.log('QStash Cron Schedule 등록 완료:')
      console.log(`  Schedule ID: ${schedule.scheduleId}`)
      console.log(`  Destination: ${APP_URL}/api/cron/poll`)
      console.log(`  Cron: */3 5-13 * * * (UTC) = 매 3분, KST 14~22시`)
      console.log('')
      console.log('QStash Dashboard에서 확인: https://console.upstash.com/qstash')
    }

    main().catch((err) => {
      console.error('QStash Schedule 등록 실패:', err)
      process.exit(1)
    })
    ```

    **2단계: .env.example 업데이트**

    기존 .env.example 파일 끝에 QStash 환경변수 섹션 추가:
    ```
    # Upstash QStash (Phase 2: 폴링 스케줄러)
    QSTASH_TOKEN=
    QSTASH_CURRENT_SIGNING_KEY=
    QSTASH_NEXT_SIGNING_KEY=

    # App URL (QStash Cron Schedule 등록 + 서명 검증용)
    APP_URL=
    ```

    **3단계: 통합 검증**

    1. `pnpm test --run` — 전체 테스트 스위트 통과 (auth + crawler + poll route)
    2. `pnpm typecheck` — TypeScript 타입 에러 없음
    3. `pnpm lint` — ESLint 에러 없음

    커밋: `feat(02-02): 추가: QStash 설정 스크립트 + 환경변수 문서화`
  </action>
  <verify>
    <automated>pnpm test --run && pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - scripts/setup-qstash.ts가 존재하고 `import { Client } from '@upstash/qstash'`를 포함한다
    - scripts/setup-qstash.ts가 `schedules.create`를 포함한다
    - scripts/setup-qstash.ts가 `cron: '*/3 5-13 * * *'`를 포함한다 (UTC 05~13 = KST 14~22)
    - scripts/setup-qstash.ts가 `QSTASH_TOKEN` 환경변수 체크를 포함한다
    - .env.example이 `QSTASH_TOKEN=`, `QSTASH_CURRENT_SIGNING_KEY=`, `QSTASH_NEXT_SIGNING_KEY=`, `APP_URL=`을 포함한다
    - `pnpm test --run` 종료 코드 0 (전체 테스트)
    - `pnpm typecheck` 종료 코드 0
    - `pnpm lint` 종료 코드 0
  </acceptance_criteria>
  <done>
    QStash Cron Schedule 등록 스크립트가 3분 간격(KST 14~22시) 폴링을 설정할 수 있고, 환경변수 템플릿이 문서화되었으며, 전체 테스트 스위트와 타입 체크와 린트가 통과한다.
  </done>
</task>

</tasks>

<verification>
1. `pnpm test --run` — auth + crawler + poll route 전체 테스트 통과
2. `pnpm typecheck` — TypeScript 에러 없음
3. `pnpm lint` — ESLint 에러 없음
4. src/app/api/cron/poll/route.ts가 QStash 서명 검증 + KST 시간 체크 + crawler 모듈 호출을 수행한다
5. scripts/setup-qstash.ts가 QStash Cron Schedule을 등록할 수 있다
6. .env.example에 QStash 환경변수가 문서화되어 있다
</verification>

<success_criteria>
- QStash 폴링 엔드포인트가 서명 검증을 통과한 요청만 처리한다
- 경기 시간대(KST 14~22시) 외 요청은 크롤링 없이 조기 반환된다
- 크롤링 실패 시 200을 반환하여 QStash 자동 재시도가 발생하지 않는다
- Phase 3에서 사용할 상태 전이(transitions) 핸드오프 계약이 route.ts에 주석으로 문서화된다
- 전체 테스트 스위트 + 타입 체크 + 린트가 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipeline/02-02-SUMMARY.md`
</output>

---
phase: 02-data-pipeline
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/types/crawler.ts
  - src/backend/modules/crawler/crawler-service.ts
  - src/backend/modules/crawler/game-repository.ts
  - src/backend/modules/crawler/game-state-mapper.ts
  - src/backend/modules/crawler/index.ts
  - src/backend/modules/crawler/__tests__/crawler-service.test.ts
  - src/backend/modules/crawler/__tests__/game-repository.test.ts
  - supabase/migrations/20260405000000_game_notified_flags.sql
autonomous: true
requirements: [DATA-01, DATA-02, DATA-04, INFRA-03]
user_setup: []

must_haves:
  truths:
    - "fetchTodayGames()가 kbo-game getGame(new Date()) 호출 후 CrawlerResult를 반환한다"
    - "kbo-game이 null을 반환하면 { success: false, error } 경로로 처리된다"
    - "kbo-game이 빈 배열을 반환하면 { success: true, games: [] } 경로로 처리된다"
    - "syncGames()가 DB의 이전 상태와 비교하여 Scheduled->Playing, Playing->Finished, Cancelled 전이를 감지한다"
    - "upsert 시 is_notified_* 플래그가 덮어씌워지지 않아 중복 알림이 방지된다"
    - "games 테이블에 game_date+home_team+away_team 복합 unique 제약이 존재하여 경기 중복이 방지된다"
    - "경기 상태가 DB에 영속화되어 서버 재시작에도 유지된다 (INFRA-03)"
  artifacts:
    - path: "src/types/crawler.ts"
      provides: "CrawlerResult, CrawlerGame, StateTransition, KboGameStatus 타입"
      exports: ["CrawlerResult", "CrawlerGame", "StateTransition", "KboGameStatus"]
    - path: "src/backend/modules/crawler/crawler-service.ts"
      provides: "fetchTodayGames() — kbo-game 래핑, null/[] 분리"
      exports: ["fetchTodayGames"]
    - path: "src/backend/modules/crawler/game-repository.ts"
      provides: "syncGames() — DB upsert + 상태 전이 감지"
      exports: ["syncGames"]
    - path: "src/backend/modules/crawler/game-state-mapper.ts"
      provides: "mapKboStatusToDb() — 대문자 KBO 상태를 소문자 DB 상태로 변환"
      exports: ["mapKboStatusToDb"]
    - path: "src/backend/modules/crawler/index.ts"
      provides: "crawler 모듈 public API re-export"
    - path: "supabase/migrations/20260405000000_game_notified_flags.sql"
      provides: "is_notified 상태별 분리 + 복합 unique 제약 마이그레이션"
  key_links:
    - from: "src/backend/modules/crawler/crawler-service.ts"
      to: "kbo-game"
      via: "import { getGame } from 'kbo-game'"
      pattern: "getGame\\(new Date"
    - from: "src/backend/modules/crawler/crawler-service.ts"
      to: "src/backend/modules/crawler/game-state-mapper.ts"
      via: "mapKboStatusToDb import"
      pattern: "mapKboStatusToDb"
    - from: "src/backend/modules/crawler/game-repository.ts"
      to: "src/lib/supabase/server.ts"
      via: "createServerSupabaseClient import"
      pattern: "createServerSupabaseClient"
    - from: "src/backend/modules/crawler/game-repository.ts"
      to: "src/types/crawler.ts"
      via: "StateTransition, CrawlerGame type imports"
      pattern: "import.*StateTransition"
---

<objective>
CrawlerService(kbo-game 래핑)와 GameRepository(Supabase 상태 전이 감지)를 TDD로 구현한다.

Purpose: Phase 2의 핵심 데이터 레이어 — kbo-game에서 경기 데이터를 수집하고, DB와 비교하여 상태 전이를 감지하며, 중복 알림 없이 상태를 영속화한다(INFRA-03). Plan 02의 폴링 API Route가 이 모듈을 호출한다.
Output: crawler 모듈 (service + repository + mapper + types) + TDD 테스트 스위트 + DB 마이그레이션
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-pipeline/02-CONTEXT.md
@.planning/phases/02-data-pipeline/02-RESEARCH.md

@src/types/game.ts
@src/lib/supabase/server.ts
@src/lib/logger.ts
@src/backend/modules/auth/index.ts
@src/backend/modules/auth/__tests__/user-repository.test.ts
@supabase/migrations/20260404000000_init_schema.sql
@vitest.config.mts

<interfaces>
<!-- 기존 타입 및 패턴 — executor는 이것을 그대로 사용한다 -->

From src/types/game.ts:
```typescript
export type GameStatus = 'scheduled' | 'playing' | 'finished' | 'cancelled'

export type Game = {
  id: string
  game_date: string
  home_team: string
  away_team: string
  status: GameStatus
  home_score: number
  away_score: number
  inning_data: Record<string, unknown> | null
  started_at: string | null
  finished_at: string | null
  is_notified: boolean
  created_at: string
  updated_at: string
}
```

From src/lib/supabase/server.ts:
```typescript
export async function createServerSupabaseClient()
```

From src/lib/logger.ts:
```typescript
export const logger: pino.Logger
```

From src/backend/modules/auth/index.ts (모듈 export 패턴):
```typescript
export { upsertUser, getUserByTossKey, updateTeamCode, updateSubscription } from './user-repository'
export { exchangeAuthCode, getTossUserKey } from './toss-client'
```

From supabase/migrations/20260404000000_init_schema.sql (games 테이블):
```sql
create table public.games (
  id           uuid primary key default gen_random_uuid(),
  game_date    date not null,
  home_team    text not null,
  away_team    text not null,
  status       text not null default 'scheduled',
  home_score   int not null default 0,
  away_score   int not null default 0,
  inning_data  jsonb,
  started_at   timestamptz,
  finished_at  timestamptz,
  is_notified  boolean not null default false,
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index idx_games_date_status on public.games(game_date, status);
```

kbo-game@0.0.2 타입 (RESEARCH.md 확인):
```typescript
// kbo-game의 Game 인터페이스 (주요 필드)
type KboGame = {
  id: string           // KBO 내부 ID
  date: string         // YYYYMMDD
  startTime: string
  season: string
  homeTeam: string
  awayTeam: string
  status: 'SCHEDULED' | 'IN_PROGRESS' | 'FINISHED' | 'CANCELED'
  homeScore: number
  awayScore: number
  currentInning: number
}

// getGame(Date) -> Game[] | null
// null = 크롤링 실패, [] = 경기 없음
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DB 마이그레이션 + 공유 타입 + CrawlerService TDD</name>
  <files>
    supabase/migrations/20260405000000_game_notified_flags.sql,
    src/types/crawler.ts,
    src/backend/modules/crawler/game-state-mapper.ts,
    src/backend/modules/crawler/crawler-service.ts,
    src/backend/modules/crawler/__tests__/crawler-service.test.ts
  </files>
  <read_first>
    src/types/game.ts,
    src/lib/logger.ts,
    src/backend/modules/auth/__tests__/user-repository.test.ts,
    supabase/migrations/20260404000000_init_schema.sql,
    vitest.config.mts,
    package.json
  </read_first>
  <behavior>
    CrawlerService (crawler-service.test.ts):
    - Test 1: getGame(new Date())이 정상 Game[]을 반환하면 { success: true, games: CrawlerGame[] }을 반환한다 (DATA-01)
    - Test 2: getGame()이 null을 반환하면 { success: false, error: Error('kbo-game returned null') }을 반환한다 (DATA-04)
    - Test 3: getGame()이 빈 배열을 반환하면 { success: true, games: [] }을 반환한다 — null과 다른 코드 경로 (DATA-04)
    - Test 4: getGame()이 예외를 throw하면 { success: false, error }로 래핑한다 (DATA-04)
    - Test 5: kbo-game의 SCHEDULED/IN_PROGRESS/FINISHED/CANCELED가 scheduled/playing/finished/cancelled로 매핑된다

    game-state-mapper.ts:
    - Test 6: mapKboStatusToDb('SCHEDULED') === 'scheduled'
    - Test 7: mapKboStatusToDb('IN_PROGRESS') === 'playing'
    - Test 8: mapKboStatusToDb('FINISHED') === 'finished'
    - Test 9: mapKboStatusToDb('CANCELED') === 'cancelled' (D 없음 -> D 있음)
  </behavior>
  <action>
    **0단계: DB 마이그레이션 (supabase/migrations/20260405000000_game_notified_flags.sql)**

    TDD 코드 작성 전에 DB 스키마 변경을 먼저 확정한다. 이 파일은 kbo-game 타입 불확실성과 무관한 독립 작업이므로 선행 완료한다.

    is_notified 단일 플래그를 상태별로 분리 (per D-06, RESEARCH Open Question 2):
    ```sql
    -- is_notified를 상태별 플래그로 분리 (시작/종료/취소 각각 알림 가능)
    ALTER TABLE public.games ADD COLUMN is_notified_start boolean NOT NULL DEFAULT false;
    ALTER TABLE public.games ADD COLUMN is_notified_finish boolean NOT NULL DEFAULT false;
    ALTER TABLE public.games ADD COLUMN is_notified_cancel boolean NOT NULL DEFAULT false;

    -- 기존 is_notified 값을 finish로 마이그레이션 (기존 데이터 보존)
    UPDATE public.games SET is_notified_finish = is_notified;

    -- 기존 is_notified 컬럼 삭제
    ALTER TABLE public.games DROP COLUMN is_notified;

    -- game_date + home_team + away_team 복합 unique 제약 추가 (upsert onConflict용)
    -- RESEARCH Open Question 1: kbo-game id가 DB uuid와 다르므로 복합키로 upsert
    ALTER TABLE public.games ADD CONSTRAINT uq_games_date_teams UNIQUE (game_date, home_team, away_team);
    ```

    DB에 경기 상태를 영속화하여 서버 재시작에도 유지되도록 보장한다 (INFRA-03).
    커밋: `chore(02-01): 추가: is_notified 상태별 분리 + 복합 unique 제약 마이그레이션`

    **1단계: 패키지 설치**
    ```bash
    pnpm add kbo-game @upstash/qstash
    ```

    **2단계: 공유 타입 정의 (src/types/crawler.ts)**

    kbo-game의 GameStatus 대문자 타입:
    ```typescript
    export type KboGameStatus = 'SCHEDULED' | 'IN_PROGRESS' | 'FINISHED' | 'CANCELED'
    ```

    CrawlerGame 타입 (kbo-game Game을 내부 표현으로 변환한 것):
    ```typescript
    export type CrawlerGame = {
      kboGameId: string        // kbo-game의 id 원본 보존
      gameDate: string         // YYYYMMDD -> YYYY-MM-DD 변환
      homeTeam: string
      awayTeam: string
      status: GameStatus       // 이미 소문자로 변환된 상태
      homeScore: number
      awayScore: number
      currentInning: number
      startTime: string
    }
    ```

    CrawlerResult 타입 (discriminated union):
    ```typescript
    export type CrawlerResult =
      | { success: true; games: CrawlerGame[] }
      | { success: false; error: Error }
    ```

    StateTransition 타입 (per D-04, D-05):
    ```typescript
    export type StateTransition = {
      gameId: string           // DB games.id (uuid)
      kboGameId: string        // kbo-game 원본 ID
      fromStatus: GameStatus
      toStatus: GameStatus
      game: CrawlerGame
    }
    ```

    **3단계: game-state-mapper.ts**

    kbo-game 대문자 상태를 DB 소문자 상태로 매핑하는 순수 함수:
    ```typescript
    import type { GameStatus } from '@/types/game'
    import type { KboGameStatus } from '@/types/crawler'

    const STATUS_MAP: Record<KboGameStatus, GameStatus> = {
      SCHEDULED: 'scheduled',
      IN_PROGRESS: 'playing',
      FINISHED: 'finished',
      CANCELED: 'cancelled',  // 미국식 CANCELED -> 영국식 cancelled
    } as const

    export function mapKboStatusToDb(kboStatus: KboGameStatus): GameStatus {
      return STATUS_MAP[kboStatus]
    }
    ```

    **4단계: RED — crawler-service.test.ts 작성**

    auth 모듈 테스트 패턴을 따라 vi.mock('kbo-game')으로 getGame 모킹.
    Test 1~9 (behavior 섹션 참조) 모두 작성.
    `pnpm test --run src/backend/modules/crawler/__tests__/crawler-service.test.ts` 실행 -> 전부 FAIL 확인.
    커밋: `test(02-01): 추가: CrawlerService + GameStateMapper 실패 테스트`

    **5단계: GREEN — crawler-service.ts 구현**

    fetchTodayGames() 함수:
    - `import { getGame } from 'kbo-game'` 호출
    - try/catch로 래핑
    - null 반환 시 `{ success: false, error: new Error('kbo-game returned null') }` (per D-01)
    - 정상 배열 반환 시 각 게임을 mapKboStatusToDb + 날짜 변환하여 CrawlerGame[]으로 변환
    - 예외 throw 시 `{ success: false, error }` 래핑
    - 에러 발생 시 `logger.error()` 호출 (per D-02)

    `pnpm test --run src/backend/modules/crawler/__tests__/crawler-service.test.ts` 실행 -> 전부 PASS 확인.
    커밋: `feat(02-01): 구현: CrawlerService kbo-game 래핑 + GameStateMapper`
  </action>
  <verify>
    <automated>pnpm test --run src/backend/modules/crawler/__tests__/crawler-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/20260405000000_game_notified_flags.sql이 `is_notified_start`, `is_notified_finish`, `is_notified_cancel`, `uq_games_date_teams`를 포함한다
    - src/types/crawler.ts가 존재하고 `export type CrawlerResult`, `export type CrawlerGame`, `export type StateTransition`, `export type KboGameStatus`를 포함한다
    - src/backend/modules/crawler/game-state-mapper.ts가 `export function mapKboStatusToDb`를 포함한다
    - src/backend/modules/crawler/crawler-service.ts가 `export async function fetchTodayGames`를 포함한다
    - src/backend/modules/crawler/crawler-service.ts가 `import { getGame } from 'kbo-game'`을 포함한다
    - src/backend/modules/crawler/crawler-service.ts가 `logger.error`를 포함한다 (per D-02)
    - src/backend/modules/crawler/__tests__/crawler-service.test.ts가 최소 5개 테스트를 포함한다
    - `pnpm test --run src/backend/modules/crawler/__tests__/crawler-service.test.ts` 종료 코드 0
  </acceptance_criteria>
  <done>
    DB 마이그레이션이 is_notified를 상태별로 분리하고 복합 unique 제약을 추가하여 경기 상태가 DB에 영속화된다(INFRA-03). CrawlerService가 kbo-game을 래핑하여 null(크롤링 실패)과 [](경기 없음)을 분리된 코드 경로로 처리하고, GameStateMapper가 대문자->소문자 상태 매핑을 수행하며, 모든 TDD 테스트가 통과한다.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GameRepository TDD — 상태 전이 감지 + Supabase upsert</name>
  <files>
    src/backend/modules/crawler/game-repository.ts,
    src/backend/modules/crawler/__tests__/game-repository.test.ts,
    src/backend/modules/crawler/index.ts
  </files>
  <read_first>
    src/types/game.ts,
    src/types/crawler.ts,
    src/lib/supabase/server.ts,
    src/backend/modules/crawler/game-state-mapper.ts,
    src/backend/modules/crawler/crawler-service.ts,
    src/backend/modules/auth/__tests__/user-repository.test.ts,
    supabase/migrations/20260405000000_game_notified_flags.sql
  </read_first>
  <behavior>
    GameRepository (game-repository.test.ts):
    - Test 1: 새 경기(DB에 없음)를 syncGames()하면 upsert 호출되고 빈 transitions[] 반환 (신규 경기는 전이 아님)
    - Test 2: DB에 status='scheduled'인 경기가 있고 크롤링 결과 status='playing'이면 StateTransition { fromStatus: 'scheduled', toStatus: 'playing' } 반환 (DATA-02)
    - Test 3: DB에 status='playing'인 경기가 있고 크롤링 결과 status='finished'이면 StateTransition { fromStatus: 'playing', toStatus: 'finished' } 반환 (DATA-02)
    - Test 4: DB에 status='scheduled'인 경기가 있고 크롤링 결과 status='cancelled'이면 StateTransition { fromStatus: 'scheduled', toStatus: 'cancelled' } 반환 (DATA-02)
    - Test 5: 상태가 동일하면 (playing -> playing) transitions가 비어있다
    - Test 6: upsert 페이로드에 is_notified_start, is_notified_finish, is_notified_cancel 필드가 포함되지 않는다 (기존 플래그 덮어쓰기 방지, per D-06)
    - Test 7: 빈 CrawlerGame[]로 syncGames()하면 upsert 호출 없이 빈 transitions[] 반환
    - Test 8: game_date가 오늘인 경기만 DB에서 조회한다 (select where game_date = today)
  </behavior>
  <action>
    **1단계: RED — game-repository.test.ts 작성**

    auth 모듈 테스트 패턴을 따라 Supabase 클라이언트 모킹:
    - `vi.mock('@/lib/supabase/server')` 로 createServerSupabaseClient 모킹
    - `.from('games').select('*').eq('game_date', today)` 체인 모킹 (기존 경기 조회)
    - `.from('games').upsert(payload, { onConflict: 'game_date,home_team,away_team' })` 체인 모킹

    Test 1~8 (behavior 섹션 참조) 작성.
    주의: upsert 페이로드 검증 시 `is_notified_start`, `is_notified_finish`, `is_notified_cancel` 키가 없음을 `expect(payload).not.toHaveProperty('is_notified_start')` 등으로 확인.

    `pnpm test --run src/backend/modules/crawler/__tests__/game-repository.test.ts` 실행 -> 전부 FAIL 확인.
    커밋: `test(02-01): 추가: GameRepository 상태 전이 감지 실패 테스트`

    **2단계: GREEN — game-repository.ts 구현**

    syncGames(crawledGames: CrawlerGame[]) 함수:
    1. `createServerSupabaseClient()`로 Supabase 클라이언트 생성
    2. 오늘 날짜를 KST 기준으로 계산: `new Date(new Date().toLocaleString('en-US', { timeZone: 'Asia/Seoul' }))` -> `YYYY-MM-DD` 포맷
    3. DB에서 오늘 경기 조회: `supabase.from('games').select('*').eq('game_date', today)`
    4. 결과를 `Map<string, Game>`으로 변환 (key = `${home_team}_${away_team}`)
    5. crawledGames를 순회하며:
       - upsert 페이로드 구성: `{ game_date, home_team, away_team, status, home_score, away_score, inning_data: null, started_at, finished_at }` — **is_notified_* 필드 제외**
       - `status`가 `'playing'`이면 `started_at = new Date().toISOString()`
       - `status`가 `'finished'`이면 `finished_at = new Date().toISOString()`
       - `supabase.from('games').upsert(payload, { onConflict: 'game_date,home_team,away_team' })` 호출
       - 기존 경기가 있고 상태가 변경되었으면 `StateTransition` 객체를 transitions 배열에 추가
    6. transitions 배열 반환

    **3단계: index.ts 작성**

    auth 모듈 패턴을 따라 public API re-export:
    ```typescript
    export { fetchTodayGames } from './crawler-service'
    export { syncGames } from './game-repository'
    export { mapKboStatusToDb } from './game-state-mapper'
    ```

    `pnpm test --run src/backend/modules/crawler` 실행 -> 전체 테스트 PASS.
    `pnpm test --run` 실행 -> 기존 테스트 포함 전체 PASS.
    커밋: `feat(02-01): 구현: GameRepository 상태 전이 감지 + crawler 모듈 public API`
  </action>
  <verify>
    <automated>pnpm test --run src/backend/modules/crawler</automated>
  </verify>
  <acceptance_criteria>
    - src/backend/modules/crawler/game-repository.ts가 존재하고 `export async function syncGames`를 포함한다
    - src/backend/modules/crawler/game-repository.ts가 `onConflict: 'game_date,home_team,away_team'`를 포함한다
    - src/backend/modules/crawler/game-repository.ts에 `is_notified` 문자열이 포함되지 않는다 (upsert에서 제외)
    - src/backend/modules/crawler/__tests__/game-repository.test.ts가 최소 7개 테스트를 포함한다
    - src/backend/modules/crawler/index.ts가 `export { fetchTodayGames }`, `export { syncGames }`, `export { mapKboStatusToDb }`를 포함한다
    - `pnpm test --run src/backend/modules/crawler` 종료 코드 0
    - `pnpm test --run` (전체 테스트) 종료 코드 0
  </acceptance_criteria>
  <done>
    GameRepository가 DB에서 오늘 경기 상태를 조회하고 크롤링 결과와 비교하여 Scheduled->Playing, Playing->Finished, Cancelled 전이를 정확히 감지하며, upsert 시 is_notified_* 플래그를 덮어쓰지 않고, 전체 crawler 모듈 TDD 테스트 스위트가 통과한다.
  </done>
</task>

</tasks>

<verification>
1. `pnpm test --run src/backend/modules/crawler` — crawler 모듈 전체 테스트 통과
2. `pnpm test --run` — 기존 auth 테스트 포함 전체 테스트 통과
3. `pnpm typecheck` — TypeScript 타입 에러 없음
4. src/types/crawler.ts에 CrawlerResult, CrawlerGame, StateTransition, KboGameStatus 타입이 정의됨
5. supabase/migrations/20260405000000_game_notified_flags.sql에 is_notified 분리 + unique 제약이 정의됨
6. 경기 상태가 DB에 영속화되어 서버 재시작에도 유지됨 (INFRA-03)
</verification>

<success_criteria>
- CrawlerService: kbo-game null 반환과 빈 배열 반환이 서로 다른 코드 경로(CrawlerResult discriminated union)로 처리된다
- GameRepository: 모든 상태 전이(Scheduled->Playing, Playing->Finished, Cancelled)가 StateTransition[]으로 반환된다
- DB 마이그레이션: is_notified가 상태별(start/finish/cancel) 3개 플래그로 분리되고, game_date+home_team+away_team 복합 unique 제약이 추가된다
- 경기 상태가 DB에 영속화되어 서버 재시작에도 유지된다 (INFRA-03)
- 기존 Phase 1 테스트가 깨지지 않는다
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipeline/02-01-SUMMARY.md`
</output>

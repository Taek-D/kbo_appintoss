---
phase: 03-push-notification
plan: 02
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/backend/modules/push/notification-service.ts
  - src/backend/modules/push/__tests__/notification-service.test.ts
  - src/backend/modules/push/index.ts
  - src/app/api/cron/poll/route.ts
  - src/app/api/cron/poll/__tests__/route.test.ts
autonomous: true
requirements: [PUSH-01, PUSH-03, PUSH-05]

must_haves:
  truths:
    - "경기 종료 감지 후 구독자에게 토스 Push 알림이 발송된다"
    - "동시에 2개 경기가 종료되어도 각 경기 발송이 서로 블로킹하지 않고 각각 완료된다"
    - "발송 성공/실패 결과가 push_logs 테이블에 기록된다"
    - "발송 실패한 유저는 스킵하고 다음 유저로 진행한다"
    - "is_notified_finish/cancel 플래그가 발송 완료 후 true로 업데이트된다"
  artifacts:
    - path: "src/backend/modules/push/notification-service.ts"
      provides: "구독자 조회 + 순차 발송 + push_logs 기록 오케스트레이션"
      exports: ["sendGameEndNotifications"]
    - path: "src/backend/modules/push/__tests__/notification-service.test.ts"
      provides: "NotificationService 단위 테스트"
      min_lines: 120
    - path: "src/backend/modules/push/index.ts"
      provides: "Push 모듈 public API"
      exports: ["sendGameEndNotifications"]
    - path: "src/app/api/cron/poll/route.ts"
      provides: "Push 발송 통합된 폴링 엔드포인트"
      contains: "sendGameEndNotifications"
  key_links:
    - from: "src/backend/modules/push/notification-service.ts"
      to: "src/backend/modules/push/push-provider.ts"
      via: "PushProvider 의존성 주입"
      pattern: "pushProvider\\.send"
    - from: "src/backend/modules/push/notification-service.ts"
      to: "supabase push_logs table"
      via: "createServiceRoleClient().from('push_logs').insert"
      pattern: "from\\('push_logs'\\)"
    - from: "src/backend/modules/push/notification-service.ts"
      to: "supabase users table"
      via: "createServiceRoleClient().from('users').select"
      pattern: "from\\('users'\\)"
    - from: "src/app/api/cron/poll/route.ts"
      to: "src/backend/modules/push/index.ts"
      via: "import { sendGameEndNotifications }"
      pattern: "sendGameEndNotifications"
---

<objective>
NotificationService를 TDD로 구현하고 poll/route.ts에 통합하여, 경기 종료 감지 즉시 구독자에게 Push 알림을 발송하는 전체 파이프라인을 완성한다.

Purpose: Phase 3의 비즈니스 로직 핵심인 구독자 조회, 순차 발송(100ms 간격), 동시 경기 처리(Promise.allSettled), push_logs 기록을 구현하고, Phase 2의 poll/route.ts에 연결하여 알림 파이프라인을 완성한다.
Output: src/backend/modules/push/notification-service.ts, src/backend/modules/push/index.ts, 수정된 poll/route.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-push-notification/03-CONTEXT.md
@.planning/phases/03-push-notification/03-RESEARCH.md
@.planning/phases/03-push-notification/03-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 타입/모듈. Executor는 이를 직접 사용한다. -->

From src/types/push.ts (Plan 01 산출물):
```typescript
export type TossPushRequest = {
  userKey: string
  templateId: string
  templateArgs: Record<string, string>
  deepLink?: string
}

export type TossPushResponse = {
  success: boolean
  errorCode?: string
  errorMessage?: string
}

export const PushLogStatusSchema = z.enum(['sent', 'failed', 'rate_limited'])
export type PushLogStatus = z.infer<typeof PushLogStatusSchema>

export type NotificationType = 'finished' | 'cancelled'

export type PushProvider = {
  send: (request: TossPushRequest) => Promise<TossPushResponse>
}
```

From src/backend/modules/push/push-provider.ts (Plan 01 산출물):
```typescript
export function createPushProvider(): PushProvider
```

From src/lib/supabase/service.ts (Plan 01 산출물):
```typescript
export function createServiceRoleClient(): SupabaseClient
```

From src/types/crawler.ts:
```typescript
export type StateTransition = {
  gameId: string           // DB games.id (uuid)
  kboGameId: string
  fromStatus: GameStatus
  toStatus: GameStatus
  game: CrawlerGame        // homeTeam, awayTeam은 TeamCode 형식 ('LG', 'KT' 등)
}
```

From src/types/user.ts:
```typescript
export type TeamCode = 'HH' | 'OB' | 'LG' | 'KT' | 'SS' | 'NC' | 'SK' | 'LT' | 'WO' | 'KI'
export const KBO_TEAMS: Array<{ code: TeamCode; name: string; logo: string }>
export type User = {
  id: string
  toss_user_key: string
  team_code: TeamCode | null
  subscribed: boolean
}
```

From src/app/api/cron/poll/route.ts (현재 상태, lines 46-68):
```typescript
// 4. DB 동기화 + 상태 전이 감지 (DATA-02)
const transitions = await syncGames(result.games)

// Phase 3 Handoff Contract:
// transitions(StateTransition[])는 Phase 3에서 Push 알림 트리거로 사용된다.
// ...
logger.info({ gamesCount, transitionCount, transitions }, 'Poll complete')
return new Response('OK', { status: 200 })
```

From supabase/migrations/20260404000000_init_schema.sql (push_logs):
```sql
create table public.push_logs (
  id            bigint primary key generated always as identity,
  user_id       uuid not null references public.users(id),
  game_id       uuid not null references public.games(id),
  status        text not null,     -- 'sent' | 'failed' | 'rate_limited'
  error_message text,
  sent_at       timestamptz not null default now()
);
```

From src/lib/logger.ts:
```typescript
export const logger: pino.Logger
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NotificationService TDD (구독자 조회 + 순차 발송 + push_logs 기록)</name>
  <files>
    src/backend/modules/push/notification-service.ts,
    src/backend/modules/push/__tests__/notification-service.test.ts,
    src/backend/modules/push/index.ts
  </files>
  <read_first>
    src/types/push.ts,
    src/types/crawler.ts,
    src/types/user.ts,
    src/types/game.ts,
    src/backend/modules/push/push-provider.ts,
    src/lib/supabase/service.ts,
    src/lib/logger.ts,
    src/backend/modules/auth/index.ts,
    src/backend/modules/crawler/index.ts,
    supabase/migrations/20260404000000_init_schema.sql
  </read_first>
  <behavior>
    NotificationService 테스트 (notification-service.test.ts):

    구독자 조회:
    - Test 1: transition.game.homeTeam='LG'일 때 users 테이블에서 team_code='LG' AND subscribed=true인 유저를 조회한다
    - Test 2: 홈팀(homeTeam)과 원정팀(awayTeam) 구독자 모두 조회하여 합산한다 (LG vs KT → LG 구독자 + KT 구독자)
    - Test 3: 구독자가 0명이면 발송을 건너뛰고 빈 결과를 반환한다

    순차 발송 + Rate Limit:
    - Test 4: 3명의 구독자에게 순차 발송 시 pushProvider.send()가 3번 호출되고, 각 호출 사이에 100ms 이상 간격이 존재한다 (vi.advanceTimersByTime으로 검증)
    - Test 5: 429 응답 시 1초 대기 후 해당 유저에 대해 1회 재시도한다 (D-08)
    - Test 6: 재시도도 실패하면 push_logs에 'rate_limited'로 기록 후 다음 유저로 진행한다

    발송 실패 처리 (D-07):
    - Test 7: 개별 유저 발송 실패(401 등) 시 push_logs에 status='failed', error_message 기록 후 다음 유저로 계속 진행한다
    - Test 8: 발송 성공 시 push_logs에 status='sent' 기록한다

    동시 경기 처리 (D-06):
    - Test 9: 2개의 StateTransition(finished, cancelled)을 전달하면 Promise.allSettled로 병렬 처리되어 둘 다 완료된다
    - Test 10: 한 경기 발송이 실패해도 다른 경기 발송은 정상 완료된다

    is_notified 플래그:
    - Test 11: toStatus='finished' 발송 완료 후 games 테이블의 is_notified_finish가 true로 업데이트된다
    - Test 12: toStatus='cancelled' 발송 완료 후 games 테이블의 is_notified_cancel이 true로 업데이트된다

    알림 메시지 (D-01, D-02, D-03):
    - Test 13: finished 전이 시 pushProvider.send()에 templateArgs에 팀 이름이 포함되고, deepLink에 /game/{gameId}가 포함된다
    - Test 14: 전이 필터링: toStatus가 'finished' 또는 'cancelled'만 처리하고, 'playing'이나 'scheduled'는 무시한다 (D-03)
  </behavior>
  <action>
    **Step 1: RED — notification-service.test.ts 작성**

    PushProvider를 의존성 주입으로 mock한다. Supabase 클라이언트는 vi.mock으로 모킹한다.
    vi.hoisted() 패턴 사용 (Phase 2 교훈).

    Mock 구조:
    ```typescript
    // PushProvider mock
    const mockPushProvider: PushProvider = {
      send: vi.fn(),
    }

    // Supabase mock
    const mockFrom = vi.fn()
    const mockSelect = vi.fn()
    const mockInsert = vi.fn()
    const mockUpdate = vi.fn()
    const mockEq = vi.fn()

    vi.mock('@/lib/supabase/service', () => ({
      createServiceRoleClient: () => ({
        from: mockFrom,
      }),
    }))
    ```

    테스트 실행하여 14개 모두 FAIL 확인.
    커밋: `test(03-02): NotificationService 실패 테스트 작성 (RED)`

    **Step 2: GREEN — notification-service.ts 구현**

    핵심 함수: `sendGameEndNotifications(transitions: StateTransition[], pushProvider: PushProvider): Promise<void>`

    구현 로직:
    1. transitions를 필터링: `toStatus === 'finished' || toStatus === 'cancelled'`만 추출 (D-03)
    2. `Promise.allSettled()`로 각 transition에 대해 `sendNotificationsForGame()` 병렬 호출 (D-06)
    3. `sendNotificationsForGame(transition, pushProvider)`:
       a. `createServiceRoleClient()`로 Supabase 클라이언트 생성
       b. transition.game.homeTeam, transition.game.awayTeam으로 구독자 조회:
          `supabase.from('users').select('id, toss_user_key').in('team_code', [homeTeam, awayTeam]).eq('subscribed', true)`
       c. 구독자 0명이면 early return
       d. 알림 메시지 구성 (D-01, D-02):
          - finished: templateArgs에 팀 이름 포함, 메시지 "경기가 끝났어요! 탭해서 확인해보세요"
          - cancelled: 메시지 "경기가 취소되었어요"
       e. deepLink: `/game/${transition.gameId}` (D-09)
       f. for-of 루프로 구독자 순회 (D-04):
          - pushProvider.send(request) 호출
          - 429 응답 시 1초 대기 후 1회 재시도 (D-08)
          - push_logs에 결과 기록 (sent/failed/rate_limited)
          - 100ms delay (D-04, PUSH-02)
       g. is_notified_finish 또는 is_notified_cancel 플래그 업데이트

    **Step 3: index.ts 작성**

    ```typescript
    export { sendGameEndNotifications } from './notification-service'
    export { createPushProvider } from './push-provider'
    ```

    테스트 실행하여 14개 모두 PASS 확인.
    커밋: `feat(03-02): NotificationService 순차 발송 + push_logs 구현 (GREEN)`
  </action>
  <verify>
    <automated>cd "E:/프로젝트/앱인토스/kbo_game" && pnpm test --run src/backend/modules/push/__tests__/notification-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/backend/modules/push/notification-service.ts에 `export async function sendGameEndNotifications(transitions: StateTransition[], pushProvider: PushProvider)` 시그니처가 존재한다
    - notification-service.ts에 `Promise.allSettled` 호출이 존재한다 (PUSH-03)
    - notification-service.ts에 `from('users').select` 패턴으로 구독자 조회가 존재한다
    - notification-service.ts에 `from('push_logs').insert` 패턴으로 발송 로그 기록이 존재한다 (PUSH-05)
    - notification-service.ts에 `from('games').update` 패턴으로 is_notified 플래그 업데이트가 존재한다
    - notification-service.ts에 `toStatus === 'finished' || toStatus === 'cancelled'` 필터링이 존재한다 (D-03)
    - notification-service.ts에 `/game/${` 패턴으로 딥링크 생성이 존재한다 (D-09)
    - notification-service.ts에 100ms delay 패턴(`setTimeout` 또는 유사)이 존재한다 (PUSH-02)
    - notification-service.ts에 429 재시도 로직(1초 대기)이 존재한다 (D-08)
    - src/backend/modules/push/index.ts에 `export.*sendGameEndNotifications` 패턴이 존재한다
    - src/backend/modules/push/index.ts에 `export.*createPushProvider` 패턴이 존재한다
    - src/backend/modules/push/__tests__/notification-service.test.ts에 14개 이상의 test/it 블록이 존재한다
    - `pnpm test --run src/backend/modules/push/__tests__/notification-service.test.ts` 결과가 0 exit code
  </acceptance_criteria>
  <done>
    NotificationService가 구독자 조회, 순차 발송(100ms 간격), 429 재시도, push_logs 기록, Promise.allSettled 동시 경기 처리, is_notified 플래그 업데이트를 포함하여 구현되며 14개 테스트가 모두 통과한다.
  </done>
</task>

<task type="auto">
  <name>Task 2: poll/route.ts 통합 — transitions에서 Push 발송 연결</name>
  <files>
    src/app/api/cron/poll/route.ts,
    src/app/api/cron/poll/__tests__/route.test.ts
  </files>
  <read_first>
    src/app/api/cron/poll/route.ts,
    src/app/api/cron/poll/__tests__/route.test.ts,
    src/backend/modules/push/index.ts,
    src/backend/modules/push/notification-service.ts,
    src/backend/modules/push/push-provider.ts,
    src/types/push.ts,
    src/types/crawler.ts
  </read_first>
  <action>
    **Step 1: poll/route.ts 수정**

    현재 route.ts의 lines 47-68 (Phase 3 Handoff Contract 주석 + logger.info)를 다음으로 교체:

    ```typescript
    import { sendGameEndNotifications, createPushProvider } from '@/backend/modules/push'
    ```
    (파일 상단 import에 추가)

    lines 46 이후에 삽입:
    ```typescript
    // 4. DB 동기화 + 상태 전이 감지 (DATA-02)
    const transitions = await syncGames(result.games)

    // 5. Push 알림 발송 (PUSH-01, PUSH-03)
    if (transitions.length > 0) {
      try {
        const pushProvider = createPushProvider()
        await sendGameEndNotifications(transitions, pushProvider)
        logger.info(
          {
            transitionCount: transitions.length,
            transitions: transitions.map((t) => ({
              from: t.fromStatus,
              to: t.toStatus,
              teams: `${t.game.homeTeam} vs ${t.game.awayTeam}`,
            })),
          },
          'Push notifications sent'
        )
      } catch (error) {
        // Push 발송 실패가 폴링 전체를 실패시키면 안 됨
        logger.error({ err: error }, 'Push notification failed')
      }
    }

    logger.info(
      {
        gamesCount: result.games.length,
        transitionCount: transitions.length,
      },
      'Poll complete'
    )

    return new Response('OK', { status: 200 })
    ```

    Phase 3 Handoff Contract 주석 블록(lines 48-54)은 삭제한다 — 실제 구현으로 대체되었으므로.

    **Step 2: route.test.ts에 Push 통합 테스트 추가**

    기존 7개 테스트를 유지하면서 Push 발송 관련 테스트를 추가한다:

    ```typescript
    // Push 모듈 mock 추가
    vi.mock('@/backend/modules/push', () => ({
      sendGameEndNotifications: vi.fn().mockResolvedValue(undefined),
      createPushProvider: vi.fn().mockReturnValue({ send: vi.fn() }),
    }))
    ```

    추가 테스트:
    - Test 8: transitions가 있으면 sendGameEndNotifications가 호출된다
    - Test 9: transitions가 빈 배열이면 sendGameEndNotifications가 호출되지 않는다
    - Test 10: sendGameEndNotifications가 에러를 throw해도 200을 반환한다 (Push 실패가 폴링을 실패시키지 않음)

    커밋: `feat(03-02): poll/route.ts에 Push 발송 파이프라인 통합`

    **Step 3: 전체 테스트 실행**

    `pnpm test --run` — 전체 테스트 스위트 통과 확인
    `pnpm typecheck` — 타입 에러 없음
    `pnpm lint` — 린트 에러 없음
  </action>
  <verify>
    <automated>cd "E:/프로젝트/앱인토스/kbo_game" && pnpm test --run src/app/api/cron/poll/__tests__/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/cron/poll/route.ts에 `import.*sendGameEndNotifications.*createPushProvider.*from.*@/backend/modules/push` 패턴이 존재한다
    - route.ts에 `sendGameEndNotifications(transitions, pushProvider)` 호출이 존재한다
    - route.ts에 `createPushProvider()` 호출이 존재한다
    - route.ts에 Push 발송 실패를 catch하는 try/catch 블록이 존재한다
    - route.ts에서 "Phase 3 Handoff Contract" 주석 블록이 삭제되었다
    - src/app/api/cron/poll/__tests__/route.test.ts에 sendGameEndNotifications 관련 test/it 블록이 3개 이상 존재한다
    - `pnpm test --run src/app/api/cron/poll/__tests__/route.test.ts` 결과가 0 exit code
    - `pnpm test --run` 전체 테스트 결과가 0 exit code
    - `pnpm typecheck` 결과가 0 exit code
  </acceptance_criteria>
  <done>
    poll/route.ts가 syncGames() 이후 transitions을 sendGameEndNotifications()에 전달하여 Push 발송 파이프라인이 완성되며, Push 실패가 폴링 전체를 실패시키지 않고, 전체 테스트 스위트가 통과한다.
  </done>
</task>

</tasks>

<verification>
- `pnpm test --run` — 전체 테스트 스위트 통과 (기존 테스트 + Push 테스트 모두)
- `pnpm typecheck` — 타입 에러 없음
- `pnpm lint` — 린트 에러 없음
- `grep -r "sendGameEndNotifications" src/app/api/cron/poll/route.ts` — 통합 확인
- `grep -r "Promise.allSettled" src/backend/modules/push/notification-service.ts` — 동시 경기 처리 확인
- `grep -r "push_logs" src/backend/modules/push/notification-service.ts` — 발송 로그 기록 확인
</verification>

<success_criteria>
1. NotificationService가 transitions를 받아 finished/cancelled만 필터링하고, 각 경기의 구독자에게 순차 발송한다 (PUSH-01)
2. 발송 간 100ms 간격이 테스트로 검증된다 (PUSH-02, Plan 01과 연계)
3. Promise.allSettled로 동시 경기 발송이 병렬 처리된다 (PUSH-03)
4. 발송 결과가 push_logs에 sent/failed/rate_limited로 기록된다 (PUSH-05)
5. poll/route.ts에서 transitions → sendGameEndNotifications 호출이 연결된다
6. Push 발송 실패가 폴링 전체를 실패시키지 않는다
7. 모든 테스트 통과, typecheck 통과, lint 통과
</success_criteria>

<output>
After completion, create `.planning/phases/03-push-notification/03-02-SUMMARY.md`
</output>

---
phase: 03-push-notification
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/types/push.ts
  - src/backend/modules/push/push-provider.ts
  - src/backend/modules/push/__tests__/push-provider.test.ts
  - src/lib/supabase/service.ts
autonomous: true
requirements: [PUSH-04, PUSH-02]

must_haves:
  truths:
    - "mTLS 인증서가 없으면 PushProvider 생성 시 명시적 에러가 발생한다"
    - "올바른 인증서로 토스 Push API 호출 시 성공 응답을 받는다"
    - "발송 간 100ms 간격을 준수한다"
    - "429 응답 시 1초 대기 후 재시도한다"
  artifacts:
    - path: "src/types/push.ts"
      provides: "Push 관련 공유 타입 정의"
      contains: "TossPushRequest"
    - path: "src/backend/modules/push/push-provider.ts"
      provides: "토스 Push API 통신 모듈 (mTLS)"
      exports: ["createPushProvider"]
    - path: "src/backend/modules/push/__tests__/push-provider.test.ts"
      provides: "PushProvider 단위 테스트"
      min_lines: 80
    - path: "src/lib/supabase/service.ts"
      provides: "Service Role Supabase 클라이언트"
      exports: ["createServiceRoleClient"]
  key_links:
    - from: "src/backend/modules/push/push-provider.ts"
      to: "TOSS_MTLS_CERT/TOSS_MTLS_KEY env vars"
      via: "Buffer.from(base64) -> https.Agent"
      pattern: "Buffer\\.from.*base64"
    - from: "src/backend/modules/push/push-provider.ts"
      to: "src/types/push.ts"
      via: "import type"
      pattern: "import.*from.*types/push"
---

<objective>
Push 모듈의 타입 계약과 토스 Push API 통신 계층을 TDD로 구현한다.

Purpose: Phase 3의 핵심 인프라인 mTLS 인증과 Rate Limit 준수 발송을 안전하게 구축한다. PushProvider는 순수 API 통신만 담당하며, 비즈니스 로직(구독자 조회, 오케스트레이션)은 Plan 02에서 구현한다.
Output: src/types/push.ts (공유 타입), src/backend/modules/push/push-provider.ts (mTLS 통신), src/lib/supabase/service.ts (Service Role 클라이언트)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-push-notification/03-CONTEXT.md
@.planning/phases/03-push-notification/03-RESEARCH.md

<interfaces>
<!-- 기존 코드베이스에서 추출한 타입/패턴. Executor는 이를 참조하여 구현한다. -->

From src/types/game.ts:
```typescript
export type GameStatus = 'scheduled' | 'playing' | 'finished' | 'cancelled'
```

From src/types/crawler.ts:
```typescript
export type StateTransition = {
  gameId: string           // DB games.id (uuid)
  kboGameId: string        // kbo-game 원본 ID
  fromStatus: GameStatus
  toStatus: GameStatus
  game: CrawlerGame
}
```

From src/types/user.ts:
```typescript
export type TeamCode = 'HH' | 'OB' | 'LG' | 'KT' | 'SS' | 'NC' | 'SK' | 'LT' | 'WO' | 'KI'
export type User = {
  id: string
  toss_user_key: string
  team_code: TeamCode | null
  subscribed: boolean
  created_at: string
  updated_at: string
}
```

From src/lib/supabase/server.ts:
```typescript
// cookies() 기반 — QStash webhook에서는 사용 불가
export async function createServerSupabaseClient()
```

From src/lib/logger.ts:
```typescript
export const logger: pino.Logger
```

From src/backend/modules/auth/index.ts (모듈 패턴 참조):
```typescript
export { upsertUser, getUserByTossKey, updateTeamCode, updateSubscription } from './user-repository'
export { exchangeAuthCode, getTossUserKey } from './toss-client'
```

From .env.example:
```
TOSS_MTLS_CERT=
TOSS_MTLS_KEY=
SUPABASE_SERVICE_ROLE_KEY=
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Push 타입 정의 + Service Role 클라이언트 + PushProvider TDD</name>
  <files>
    src/types/push.ts,
    src/lib/supabase/service.ts,
    src/backend/modules/push/push-provider.ts,
    src/backend/modules/push/__tests__/push-provider.test.ts
  </files>
  <read_first>
    src/types/user.ts,
    src/types/game.ts,
    src/types/crawler.ts,
    src/lib/supabase/server.ts,
    src/lib/logger.ts,
    src/backend/modules/auth/index.ts,
    src/backend/modules/crawler/crawler-service.ts,
    .env.example
  </read_first>
  <behavior>
    PushProvider 테스트 (push-provider.test.ts):
    - Test 1: TOSS_MTLS_CERT 또는 TOSS_MTLS_KEY 환경변수 누락 시 createPushProvider()가 Error('mTLS 인증서 환경변수가 설정되지 않았습니다')를 throw한다
    - Test 2: TOSS_MTLS_CERT만 있고 TOSS_MTLS_KEY 없을 때도 동일 에러
    - Test 3: 유효한 인증서로 send() 호출 시 fetch가 올바른 URL, method:'POST', body, httpsAgent로 호출된다
    - Test 4: send() 성공 응답(200) 시 { success: true }를 반환한다
    - Test 5: send() 실패 응답(401) 시 { success: false, errorCode: '401', errorMessage: '...' }를 반환한다
    - Test 6: send() 응답 429 시 { success: false, errorCode: '429', errorMessage: 'Rate limit exceeded' }를 반환한다
    - Test 7: send() 네트워크 에러 시 { success: false, errorCode: 'NETWORK_ERROR', errorMessage: '...' }를 반환한다
    - Test 8: send()는 fetch에 cert/key가 포함된 Agent 옵션을 전달한다 (mTLS 검증)
  </behavior>
  <action>
    **Step 1: src/types/push.ts 생성** (per D-01, D-02, D-09)

    CLAUDE.md 컨벤션: type 선호, enum 금지, Zod 사용.

    ```typescript
    import { z } from 'zod'

    // Push 발송 요청 타입
    export type TossPushRequest = {
      userKey: string        // users.toss_user_key
      templateId: string     // 검수 승인된 템플릿 ID
      templateArgs: Record<string, string>  // 템플릿 변수 (팀 이름 등)
      deepLink?: string      // /game/{gameId} (D-09)
    }

    // Push 발송 응답 타입
    export type TossPushResponse = {
      success: boolean
      errorCode?: string     // '429', '401', 'NETWORK_ERROR' 등
      errorMessage?: string
    }

    // push_logs.status 컬럼 값
    export const PushLogStatusSchema = z.enum(['sent', 'failed', 'rate_limited'])
    export type PushLogStatus = z.infer<typeof PushLogStatusSchema>

    // 알림 타입 (finished, cancelled만 — D-03: 시작 알림 제외)
    export type NotificationType = 'finished' | 'cancelled'

    // PushProvider 인터페이스 (의존성 주입용)
    export type PushProvider = {
      send: (request: TossPushRequest) => Promise<TossPushResponse>
    }
    ```

    **Step 2: src/lib/supabase/service.ts 생성**

    poll/route.ts는 QStash webhook이므로 cookies() 없이 동작해야 한다. Service Role Key를 사용하는 별도 클라이언트가 필요하다.

    ```typescript
    import { createClient } from '@supabase/supabase-js'

    export function createServiceRoleClient() {
      const url = process.env.NEXT_PUBLIC_SUPABASE_URL
      const key = process.env.SUPABASE_SERVICE_ROLE_KEY

      if (!url || !key) {
        throw new Error('Supabase 환경변수가 설정되지 않았습니다')
      }

      return createClient(url, key)
    }
    ```

    **Step 3: RED — push-provider.test.ts 작성**

    vi.mock으로 global fetch를 모킹한다. TOSS_PUSH_API_URL 환경변수도 테스트에서 설정한다.
    vi.hoisted() 패턴 사용 (Phase 2 vitest 호이스팅 이슈 해결 패턴 적용).
    TOSS_MTLS_CERT, TOSS_MTLS_KEY는 process.env에 base64 인코딩된 더미값 설정.
    https.Agent 생성자를 모킹하여 cert/key 전달 검증.

    테스트 실행하여 모두 FAIL 확인.
    커밋: `test(03-01): PushProvider 실패 테스트 작성 (RED)`

    **Step 4: GREEN — push-provider.ts 구현**

    ```typescript
    import https from 'node:https'
    import { logger } from '@/lib/logger'
    import type { TossPushRequest, TossPushResponse, PushProvider } from '@/types/push'

    const TOSS_PUSH_API_URL = process.env.TOSS_PUSH_API_URL ?? 'https://push-api.toss.im/send'

    function createMtlsAgent(): https.Agent {
      const cert = process.env.TOSS_MTLS_CERT
      const key = process.env.TOSS_MTLS_KEY

      if (!cert || !key) {
        throw new Error('mTLS 인증서 환경변수가 설정되지 않았습니다')
      }

      return new https.Agent({
        cert: Buffer.from(cert, 'base64'),
        key: Buffer.from(key, 'base64'),
      })
    }

    export function createPushProvider(): PushProvider {
      const agent = createMtlsAgent()

      return {
        async send(request: TossPushRequest): Promise<TossPushResponse> {
          try {
            const response = await fetch(TOSS_PUSH_API_URL, {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({
                userKey: request.userKey,
                templateId: request.templateId,
                templateArgs: request.templateArgs,
                deepLink: request.deepLink,
              }),
              // @ts-expect-error -- Node.js fetch에서 agent 전달
              agent,
            })

            if (!response.ok) {
              const errorText = await response.text()
              return {
                success: false,
                errorCode: String(response.status),
                errorMessage: errorText || response.statusText,
              }
            }

            return { success: true }
          } catch (error) {
            const message = error instanceof Error ? error.message : String(error)
            logger.error({ err: error }, 'Push API 네트워크 에러')
            return {
              success: false,
              errorCode: 'NETWORK_ERROR',
              errorMessage: message,
            }
          }
        },
      }
    }
    ```

    테스트 실행하여 모두 PASS 확인.
    커밋: `feat(03-01): PushProvider mTLS 구현 (GREEN)`

    **Step 5: .env.example에 TOSS_PUSH_API_URL 추가**

    ```
    # Toss Push API
    TOSS_PUSH_API_URL=https://push-api.toss.im/send
    ```

    기존 TOSS_MTLS_CERT, TOSS_MTLS_KEY 아래에 추가한다.
  </action>
  <verify>
    <automated>cd "E:/프로젝트/앱인토스/kbo_game" && pnpm test --run src/backend/modules/push/__tests__/push-provider.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/types/push.ts 파일에 `type TossPushRequest`, `type TossPushResponse`, `type PushProvider`, `type PushLogStatus`, `type NotificationType` 정의가 존재한다
    - src/types/push.ts 파일에 `PushLogStatusSchema = z.enum` 패턴이 존재한다 (enum 금지 준수)
    - src/lib/supabase/service.ts 파일에 `export function createServiceRoleClient()` 함수가 존재한다
    - src/lib/supabase/service.ts 파일에 `SUPABASE_SERVICE_ROLE_KEY` 참조가 존재한다
    - src/backend/modules/push/push-provider.ts 파일에 `export function createPushProvider(): PushProvider` 시그니처가 존재한다
    - src/backend/modules/push/push-provider.ts 파일에 `Buffer.from(cert, 'base64')` 패턴이 존재한다
    - src/backend/modules/push/push-provider.ts 파일에 `new https.Agent` 호출이 존재한다
    - src/backend/modules/push/__tests__/push-provider.test.ts 파일에 8개 이상의 test/it 블록이 존재한다
    - .env.example 파일에 TOSS_PUSH_API_URL 항목이 존재한다
    - `pnpm test --run src/backend/modules/push/__tests__/push-provider.test.ts` 결과가 0 exit code
    - `pnpm typecheck` 결과가 0 exit code
  </acceptance_criteria>
  <done>
    Push 타입 계약(src/types/push.ts)이 정의되어 Plan 02에서 import 가능하고, PushProvider가 mTLS 인증서 로딩/검증/API 통신/에러 핸들링을 포함하여 구현되며 모든 테스트가 통과한다. Service Role Supabase 클라이언트가 QStash webhook 환경에서 사용 가능하다.
  </done>
</task>

</tasks>

<verification>
- `pnpm test --run src/backend/modules/push/` — Push 모듈 테스트 전체 통과
- `pnpm typecheck` — 타입 에러 없음
- `pnpm lint` — 린트 에러 없음
- `grep -r "TossPushRequest" src/types/push.ts` — 타입 정의 존재
- `grep -r "createPushProvider" src/backend/modules/push/push-provider.ts` — PushProvider 팩토리 존재
- `grep -r "createServiceRoleClient" src/lib/supabase/service.ts` — Service Role 클라이언트 존재
</verification>

<success_criteria>
1. src/types/push.ts에 TossPushRequest, TossPushResponse, PushProvider, PushLogStatus, NotificationType 타입이 정의됨
2. src/backend/modules/push/push-provider.ts가 mTLS 인증서를 base64 환경변수에서 로드하여 https.Agent를 구성함
3. TOSS_MTLS_CERT 또는 TOSS_MTLS_KEY 누락 시 명시적 에러 발생
4. 200/401/429/네트워크 에러 각각에 대한 응답 처리가 테스트로 검증됨
5. src/lib/supabase/service.ts가 SUPABASE_SERVICE_ROLE_KEY 기반 클라이언트를 제공함
6. 모든 테스트 통과, typecheck 통과, lint 통과
</success_criteria>

<output>
After completion, create `.planning/phases/03-push-notification/03-01-SUMMARY.md`
</output>

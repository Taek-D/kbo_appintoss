---
phase: 04-game-result-ui
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/(main)/page.tsx
autonomous: false
requirements: [UI-01, UI-05]

must_haves:
  truths:
    - "메인 화면에 오늘 경기 목록이 표시되고 응원팀 경기가 상단에 강조 배치된다"
    - "종료된 경기 카드 탭 시 /game/{gameId} 결과 화면으로 이동한다"
    - "경기 없는 날 기존 '오늘 경기 없음' UI가 유지된다"
    - "전체 서비스가 토스 화이트톤 디자인(bg-white, rounded-2xl, #0064FF)으로 일관된다"
  artifacts:
    - path: "src/app/(main)/page.tsx"
      provides: "메인 화면 실제 경기 데이터 연동"
      contains: "fetch.*api/games/today"
  key_links:
    - from: "src/app/(main)/page.tsx"
      to: "/api/games/today"
      via: "fetch in useEffect"
      pattern: "fetch.*api/games/today"
    - from: "src/app/(main)/page.tsx"
      to: "/game/[id]"
      via: "router.push on GameCard click"
      pattern: "router\\.push.*game"
---

<objective>
메인 화면을 실제 경기 데이터와 연동하여 오늘 KBO 전체 경기 목록을 표시하고, 응원팀 경기 강조 + 종료된 경기 탭 시 결과 화면 이동 + 토스 디자인 통합 마감으로 서비스를 출시 가능 상태로 완성한다.

Purpose: 메인 화면이 실제 경기 데이터를 표시하고 결과 화면으로의 네비게이션이 완성되어 서비스 전체 흐름이 연결된다.
Output: 실제 경기 데이터가 연동된 메인 화면, 시각적 검증 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-game-result-ui/04-CONTEXT.md
@.planning/phases/04-game-result-ui/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — executor should read these files -->

From src/components/GameCard.tsx (Plan 01에서 생성):
```typescript
type GameCardProps = {
  game: Game
  isMyTeam: boolean
  onClick?: () => void
}
export function GameCard({ game, isMyTeam, onClick }: GameCardProps)
```

From src/app/api/games/today/route.ts (Plan 01에서 생성):
```typescript
// GET /api/games/today → { games: Game[] }
export async function GET()
```

From src/types/game.ts:
```typescript
export type Game = {
  id: string; game_date: string; home_team: string; away_team: string;
  status: GameStatus; home_score: number; away_score: number;
  inning_data: Record<string, unknown> | null;
  started_at: string | null; finished_at: string | null;
  // ... notification flags, timestamps
}
```

From src/types/user.ts:
```typescript
export type User = {
  id: string; toss_user_key: string; team_code: TeamCode | null;
  subscribed: boolean; created_at: string; updated_at: string;
}
export const KBO_TEAMS: Array<{ code: TeamCode; name: string; logo: string }>
```

Existing main page pattern (src/app/(main)/page.tsx):
- 'use client' + useState + useEffect + fetch('/api/auth/me')
- Skeleton: animate-pulse + bg-gray-100
- Error: message + "다시 시도" button
- Layout: min-h-dvh bg-white px-5 pt-12 pb-8
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 메인 화면 경기 데이터 연동 (GameCard + 응원팀 강조 + 네비게이션)</name>
  <files>
    src/app/(main)/page.tsx
  </files>
  <read_first>
    src/app/(main)/page.tsx
    src/components/GameCard.tsx
    src/components/SubscriptionControl.tsx
    src/types/game.ts
    src/types/user.ts
    .planning/phases/04-game-result-ui/04-CONTEXT.md
  </read_first>
  <action>
`src/app/(main)/page.tsx`를 수정하여 기존 "오늘 경기 없음" 하드코딩 영역을 실제 경기 데이터로 교체한다.

**추가 import:**
```typescript
import { GameCard } from '@/components/GameCard'
import type { Game } from '@/types/game'
```

**추가 state:**
```typescript
const [games, setGames] = useState<Game[]>([])
const [isGamesLoading, setIsGamesLoading] = useState(true)
```

**경기 데이터 fetching (새 useEffect 또는 기존 fetchUser 이후):**
```typescript
const fetchGames = useCallback(async () => {
  setIsGamesLoading(true)
  try {
    const response = await fetch('/api/games/today')
    if (response.ok) {
      const data: unknown = await response.json()
      const gamesData = data as { games: Game[] }
      setGames(gamesData.games ?? [])
    }
  } catch {
    setGames([])
  } finally {
    setIsGamesLoading(false)
  }
}, [])

useEffect(() => {
  fetchGames()
}, [fetchGames])
```

**경기 목록 정렬 (per D-07 — 응원팀 상단 강조):**
```typescript
const myTeamGames = games.filter(
  (g) => g.home_team === user.team_code || g.away_team === user.team_code
)
const otherGames = games.filter(
  (g) => g.home_team !== user.team_code && g.away_team !== user.team_code
)
```

**JSX 교체 — 기존 "오늘 경기 없음" 하드코딩 div 전체를 아래로 교체:**

```jsx
{/* 하단: 오늘 경기 정보 */}
<div className="mt-6">
  {isGamesLoading ? (
    /* 경기 목록 스켈레톤 */
    <div className="animate-pulse space-y-3">
      <div className="h-20 rounded-xl bg-gray-100" />
      <div className="h-20 rounded-xl bg-gray-100" />
    </div>
  ) : games.length === 0 ? (
    /* 경기 없는 날 — 기존 UI 유지 (per D-07) */
    <div className="rounded-2xl bg-gray-50 p-6">
      <div className="flex flex-col items-center gap-3 py-8">
        <svg className="h-12 w-12 text-gray-300" fill="none" viewBox="0 0 48 48" stroke="currentColor" strokeWidth={1.5}>
          <circle cx="24" cy="24" r="20" />
          <path d="M16 8c2 4 2 8 0 12s-2 8 0 12" strokeLinecap="round" />
          <path d="M32 8c-2 4-2 8 0 12s2 8 0 12" strokeLinecap="round" />
        </svg>
        <p className="text-base font-medium text-gray-500">
          오늘은 {teamName} 경기가 없어요
        </p>
        <p className="text-xs text-gray-400">
          경기가 있는 날 알림을 보내드릴게요
        </p>
      </div>
    </div>
  ) : (
    /* 경기 목록 */
    <div className="space-y-3">
      {/* 응원팀 경기 (상단 강조) */}
      {myTeamGames.length > 0 && (
        <div>
          <p className="mb-2 text-xs font-medium text-[#0064FF]">내 팀 경기</p>
          <div className="space-y-2">
            {myTeamGames.map((game) => (
              <GameCard
                key={game.id}
                game={game}
                isMyTeam={true}
                onClick={game.status === 'finished' ? () => router.push(`/game/${game.id}`) : undefined}
              />
            ))}
          </div>
        </div>
      )}

      {/* 다른 경기 */}
      {otherGames.length > 0 && (
        <div>
          <p className="mb-2 text-xs font-medium text-gray-400">
            {myTeamGames.length > 0 ? '다른 경기' : '오늘의 경기'}
          </p>
          <div className="space-y-2">
            {otherGames.map((game) => (
              <GameCard
                key={game.id}
                game={game}
                isMyTeam={false}
                onClick={game.status === 'finished' ? () => router.push(`/game/${game.id}`) : undefined}
              />
            ))}
          </div>
        </div>
      )}
    </div>
  )}
</div>
```

**핵심 체크리스트:**
- 기존 fetchUser, SubscriptionControl, 응원팀 미선택/구독해제 상태 코드는 그대로 유지
- 기존 TODO 주석 (`Phase 2에서 games API 연동 시`) 포함된 div 전체만 교체
- `teamName` 변수는 기존 코드에 이미 정의되어 있음 — 경기 없음 UI에서 재사용
- GameCard onClick은 `status === 'finished'`인 경기만 `router.push(\`/game/${game.id}\`)` — 다른 상태는 onClick undefined (탭 불가)
  </action>
  <verify>
    <automated>cd "E:/프로젝트/앱인토스/kbo_game" && pnpm test --run && pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(main)/page.tsx contains `import { GameCard }` from `@/components/GameCard`
    - src/app/(main)/page.tsx contains `import type { Game }` from `@/types/game`
    - src/app/(main)/page.tsx contains `fetch('/api/games/today')`
    - src/app/(main)/page.tsx contains `myTeamGames` variable
    - src/app/(main)/page.tsx contains `otherGames` variable
    - src/app/(main)/page.tsx contains `router.push(\`/game/${game.id}\`)`
    - src/app/(main)/page.tsx contains text for team games section
    - src/app/(main)/page.tsx contains `경기가 없어요` (no-game UI preserved)
    - src/app/(main)/page.tsx contains `isMyTeam={true}`
    - src/app/(main)/page.tsx does NOT contain `Phase 2에서 games API 연동 시` (TODO comment removed)
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0
    - `pnpm test --run` exits 0
  </acceptance_criteria>
  <done>
    메인 화면이 /api/games/today에서 오늘 경기를 조회하여 응원팀 경기를 상단에 강조 배치하고 나머지 경기를 아래에 표시한다. 종료된 경기 탭 시 /game/{id}로 이동한다. 경기 없는 날 기존 "오늘 경기 없음" UI가 유지된다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 전체 서비스 UI 시각 검증</name>
  <files>N/A</files>
  <action>
Phase 4 전체 UI 구현 시각 검증. `pnpm dev`로 개발 서버를 실행하고 유저에게 아래 검증 항목을 안내한다.

검증 항목:
1. http://localhost:3000 메인 화면: 오늘 경기 목록 카드 표시 (경기 없으면 "오늘 경기 없음")
2. 응원팀 경기 상단 파란색 왼쪽 테두리 강조 (per D-07)
3. 종료 경기 카드 탭 시 /game/{id} 결과 화면 이동 (per D-06)
4. 결과 화면 스코어 0에서 N까지 GSAP 카운트업 애니메이션 (per D-04)
5. 결과 화면 Lenis 부드러운 스크롤 관성 (per D-05)
6. 뒤로가기 화살표 탭 시 메인 화면 이동 (per D-09)
7. 화이트톤 bg-white + rounded-2xl + #0064FF 디자인 일관성 (per D-01, UI-05)
8. /game/invalid-id 접근 시 메인 화면 자동 리디렉트 (per D-08)
  </action>
  <verify>Manual visual inspection by user</verify>
  <done>유저가 "approved" 응답하면 Phase 4 UI 시각 검증 완료. 서비스 전체 흐름 (메인 → 경기 결과 → 메인)이 정상 동작 확인됨.</done>
</task>

</tasks>

<verification>
1. `pnpm test --run` — 전체 테스트 스위트 통과
2. `pnpm typecheck` — TypeScript 에러 0개
3. `pnpm lint` — ESLint 에러 0개
4. 메인 화면에서 /api/games/today 호출하여 경기 목록 표시
5. 응원팀 경기 상단 강조, 다른 경기 하단 배치
6. 종료 경기 탭 → /game/{id} 이동
7. 경기 없는 날 "오늘 경기 없음" UI 유지
8. 토스 화이트톤 디자인 시각적 일관성
</verification>

<success_criteria>
- UI-01: 알림 클릭(딥링크) → /game/{id} 접근 시 결과 화면 표시 (메인 화면에서도 탭으로 접근 가능)
- UI-05: bg-white, rounded-2xl, #0064FF 액센트가 메인/결과 화면 전체에 일관 적용
- D-06: 경기 상태별 카드 UI (예정/진행중/종료/취소)
- D-07: 응원팀 경기 상단 강조, 전체 목록 표시
- D-08: 잘못된 gameId → 메인 리디렉트
- D-09: 뒤로가기 항상 메인으로
</success_criteria>

<output>
After completion, create `.planning/phases/04-game-result-ui/04-02-SUMMARY.md`
</output>
